# 派单归档（从 2026-09-24 起）

> **为什么有这个目录**：`A162` 那笔账的根因——本仓此前**没有派单归档制度**（`.scratch/wisp/` 只有 `issues/`），
> 于是"编排者在派单里批过"这句话**永远不可核**（票 125 的 `4824bb8` 动过冻结件 `internal/winsec/resolve.go`，
> 实现方自述"派单更新了那半句"，而写理由那笔 `bd50c63` 比代码落地晚 22 分钟＝事后追述，我这边没有任何 `A##` 可对）。
> **从这里起，凡授予范围／解冻／例外处置的派单，正文里的授权段落在派完之后落进本目录。**
> ⚠ **规矩只有三条**：①**不回填旧的**（回填＝造新史料，此前派单一律按"不可核"处理）；②**只建不删、只追加**；
> ③本文件**不是权威来源**——权威仍是 `docs/PLAN.md`／`docs/specs/**`／票面／`docs/reports/pending-and-issues.md`，
> 这里只是"我当时到底说了什么"的凭据。**与那些文件冲突时以那些文件为准，并把这一处当缺陷上报。**
> 每份记录固定五字段：**程名 · 被派时刻 · 锚点 sha · 授权范围（含"不含哪些"）· 撤销口令**。

---

## 2026-09-24 · `acceptor-ticket119-ac2-r2b`（验收程，16:1x 派）

- **被派时刻**：`2026-09-24 16:1x +08`；锚点＝该程开工第一步 `git rev-parse HEAD` 自取（派单要求它自量并 `git cat-file -t` 自证，我不预设号）。
- **范围**：**只裁票 119 的 AC#2 复判一格**（第一轮的表 `docs/evidence/s1/119-adversarial-acceptance.md` 判 AC#2 **退回**，见其 `:37`／`:141`／`:506`）。
  AC#1／AC#3／AC#4／AC#5／AC#6／**票 119 `AC#7` 一律不裁**。顺手两问：`AC#7` 判据①"两味药必须一起下"成不成立（要读数、不许替我圆）；
  `dataroot_symlink_119_other_test.go` 那三枚用例今天在 CI 上有没有分母（要读 `ci.yml`／`scripts/portable-tests.sh` 现量、引 `file:line`、不许推理答）。
- **授权（明确给了什么）**：**只读授权。**
  - **不给**任何生产码／测试码改动权：**`internal/winsec/**` 明令一字不许改**（在飞的 `acceptor-ticket137-ac4-r1` 要求
    `git diff b1010ff..HEAD -- internal/winsec/` 为空，动一下就把那整程作废）。
  - **不给**解冻：本程无解冻、无例外处置（`A162` 那枚追认只覆盖 `4824bb8` 的 `+82/−2`，**不覆盖本程、也不覆盖 `a45b2e9` 之后的 `resolve.go`**）。
  - 唯一写件＝`docs/evidence/s1/119-ac2-r2b-acceptance.md`；翻勾与 `-done` 改名权**留在编排者**，本程不许动。
- **撤销口令**：回「**撤 r2b**」⇒ 我作废这枚程的交件（若已落地，按"追加更正"处理，不改写已提交历史），票 119 的 AC#2 退回状态照旧。
- **为什么这一程带三条反假交件的硬前置**（写在这里省得下一位重新发明）：
  前身 `acceptor-ticket119-ac1-ac2-r2` 连报两枚互不相同的"完成"（692 行／六枚号 ↔ 314 行／三枚号），
  而盘上零产物、那九枚号逐枚 `git cat-file -t` 失败（账在 `A163`）。最可能根因＝**它的 `Write`/`Edit` 被权限窗拦了、却按"成功"往下写**。
  ⇒ 本程倒过来执行：**第一枚动作必须是 `Write` 建文件＋commit＋把 `git log`／`git show --name-only` 原样输出贴进 §0；被拒就只回「我被拒了」并结束**，
  在文件真实存在之前不许产出任何结论。**不采信它顺带报的任何一条**（含那条对我有利的 `AC#7` 质疑），只作为待核问题转下来。

---

## 追加（2026-09-26 01:0x，编排者自采）：**归档范围从"授予／解冻／例外"扩到"每一枚派单正文"**

**触发这次修改的不是理念，是一次没法结的争议**：票 138 那程在自己的证据件里摆了一张"简报说 X ⇒ 现量 Y ⇒ X 错"的对照表，
我 09-25 23:2x 按字面回核票面后把那一格登成"它引用了我没说过的话"（台账 `A267③④`）。**这格后来被 138 的验收程降级为未定性**——
理由很硬：**它核得了票面（整份零枚 `CI|workflow|runner|gh run` 字样），却核不了我的派单正文，因为普通实现派单从来不入盘**，
而我自己在台账里那句"派单里没有"其实是**我对上下文的回忆**，不是一件物证。
⇒ 于是本目录原来那三条规矩里的第①条（不回填旧的）保留不变，但从本行起：**凡派出一枚程，正文一并落盘到本目录**，
   文件名 `YYYY-MM-DD-<票号>-<角色>.md`（角色＝实现／验收／修复／审计），内容至少含**原有那五字段**
   （程名 · 被派时刻 · 锚点 sha · 授权范围（含"不含哪些"）· 撤销口令）。
⇒ **为什么值得为"一句归属争议"改制度**：本仓这两天所有的假绿都长在同一处——**把"谁说的"和"什么成立"混成一句话**
   （`A265④` 转述错因、`A266②` 推错判据、`A269②` 拿自己的顺序推断当结论、`A267③` 这一格）。
   前三次我都能靠"再跑一次"兜住，这一次兜不住：**没有物证的回忆不可复算**。
⇒ 本目录仍**不是权威来源**（那句原话一字不动）：派单正文只回答"我当时到底说了什么"，**不回答"什么是对的"**。
