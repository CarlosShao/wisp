# 149 — 同一族承诺还有**三个出口没人钉**：`corrupt` 那条腿换常量 0 全组不响、新注释那句"could not begin"有反例、loop 的 `exited()` 支丢 `last.summary()`

- Status: ready-for-agent
- 来源：票 147 对抗验收件 `docs/evidence/s1/147-offset-naming-r1-accept-r1.md` 的第 3 格（本程造出来的、实现方没造的四发）
- 关联：票 144（这段代码的来路）、票 147（ⓐ 那一支已经收了，本票收它**没射程覆盖**的另外三支）
- 地界：`cmd/wisp/slo_windows.go` 的 `summary()`／`readSubjectReport` 的 corrupt 支／`collectReportWithin` 的 `exited()` 支 ＋ `cmd/wisp/slo_report_144_windows_test.go` ＋ 本票自己的证据件
  ⚠ **与票 147 同一枚文件族**⇒ 147 结线（改名 `-done`）之前不许派，两程会抢同一枚测试文件。

## 现量的形状（三发都是**验收程的读数**，〔我本轮现跑过〕的只有第 4 条；下一程一律现量，别抄这里的数）

1. **`corrupt` 那条腿零牙**：把它的取值换成常量 `0`（验收程变异编号 `B14`）⇒ **全组 0 红**。
   对照 `B15`（把 `complete` 支换成 0）⇒ `case 10` 红。⇒ **`subjectReportRead.offset` 三条腿里两枚有牙、一枚全无**，
   而票 147 的 ⓐ 只收了 `unwritten` 那一条。
2. **本票要改的那句注释已经有反例**（`cmd/wisp/slo_windows.go` 里 147 新写的字段注释）：它逐字写着 corrupt 的 offset 是
   "the decoder's own answer, which is 0 **whenever it could not begin reading a value at all**"——
   验收程实测两发**已经在读值了仍报 0**：39 字节的双逗号 `{"mode":"subject-in-tree",,"pass":true}`，以及 `'\xff' after object key`。
   ⇒ **"注释预先承诺尚未成立／已不成立的行为"在本仓有名字**（票 144 兄弟票 §E7 那一族）。留着的后果与票 147 同形：下一位会以为它成立。
3. **同一枚承诺的第二个调用点**：变异编号 `B13`——把 loop 里 `s.exited()` 那一支丢掉的 `last.summary()` 拿掉 ⇒ **0 红**。
   ⇒ 票 144／147 钉的都是"到点那句红要点名停在第几字节"，而**进程中途退出**那条出口走的是另一处装配，两处都没尺。
4. ⚠ **一枚已知等价形状，别在这里白费力气（编排者加的限定语）**：147 之后 `offset ≡ bytes` 在 `unwritten` 支上成立，
   所以"红句里两个实参对调"在那一支是**等价变异**（验收程 `B9` 实测仍印 `1950 bytes read … at offset 1950`）。
   ⇒ **不许为这一发开"要它响"的格**——那是结构上永远产不出它自己想量的读数的判据（本仓既有条）。它要问的是 `corrupt` 支，不是 `unwritten` 支。

## 为什么这不是"再抠一次文案"

前两票收的是"**读到一半**"那一支的误导；剩下这三处是**同一句承诺在别的出口上仍然说了不算**：
真被 `corrupt` 拒掉时那枚数字是常量、新写的注释把"读不下去才报 0"说成了充要条件、而进程半路死掉那条路**连点名都不点**。
⇒ 排"为什么 `wisp slo` 到点红"的人仍会被指错方向（D37：错误要映射到可行动的原因）。

## AC

- [ ] **AC#1 三发各自现量"今天响不响"，响不响决定它进哪一格**（不许沿用上面那三枚编号当凭据）。
      现量命令自己写进证据件。⇒ 全为"今天不响"时本票成立；只要有**任何一发今天已经响**，那一发**归回票 147 结线**、本票据实缩射程并在票面追加登记，**不许硬开一道已经响的格**。
- [ ] **AC#2 会响的检（硬核心）**：为 `corrupt` 那条腿造一枚**只有"腿上的取值被写死／换成与实读不符"才能触发**的断言。
      承重按本仓操作定义答一句：**摘掉本票选定的一味，是否存在一发变异从此打不红？**
      ⚠ **恒真判据是一类新假绿**：写之前先问"这一发在**未修码**上响不响"；今天什么都不响就要自己造出"改了才响"的那一发，并把**改前红句原文**与**改后同一发转绿**两枚读数都贴出来（只贴"改后全绿"＝可能那条路径一次没执行）。
- [ ] **AC#3 `exited()` 那一支：先判它到底该印什么，再决定要不要尺**。
      三种可能：ⓐ 该点名（补断言）；ⓑ 设计上就不该走到 `summary()`（那把它**改成不会静默**的形状并留一发"仍走到即红"的检）；ⓒ 说不好 ⇒ **停手上报**（D22 闸门③），不许按自己的偏好填。
- [ ] **AC#4 那句注释改成它真做到的**：射程＝corrupt 支**实际**会在哪些形状上报 0，写清楚，**不许**为了省事把"有反例"这半句删掉了事。
      ⚠ 只改注释**不算收**——AC#2 才是那枚牙；两味都要，缺一按装饰处理。
- [ ] **AC#5 契约轴**：`internal/risk/**`、`internal/panel/**`、`internal/agent/approval/**`、`tools/d22scan/**`、
      `thresholds.go`、golden、`allowlist.txt`、`scripts/slo-check.ps1`、`docs/PLAN.md`、`docs/specs/**`、
      `frontend/**`、`design/**` **一律零字节**。
      ⚠ 后两枚此刻正被另一枚会话写着——**不碰、不还原、也不把它们的状态算进任何"零命中"的宣称**。
      另：预算常量（`subjectReportBudget`/`subjectGrace`）**不许动**，拿放宽预算当修法＝放水，直接退回。
- [ ] **AC#6 门禁（一律逐包单跑）**：`go test ./cmd/wisp/` 改前改后各一次，四数之外**名册差集**（两向 `comm`）；
      跑 `cmd/wisp` 要 dll 注入（CI 同形＝`bash scripts/wisp-cli-tests.sh`），否则 `exit status 0xc0000135` ＋ **0 条 `=== RUN`**＝根本没跑到；
      `D:\work\base\gopath\bin\gofumpt.exe -l . tools/d22scan tools/mockllm` 空（**版本自己 `--version` 现读并贴出，别背数**；`gofmt` 是另一把尺）；
      `go vet ./cmd/wisp/` 空；`sh scripts/d22scan.sh` rc=0 且各作用域 `examined N` 非零。
      ⚠ 判红绿只认 `--- FAIL:`，且**别用裸 grep 数**：本仓实测汇总行与 `-skip` 散文都会造出假命中（票 147 那一族两次中招）。

## 这一枚票**不**解决的事（登记，别顺手扩大）

- 它**不**重开票 147 的 `unwritten` 支（那一支已按 ⓐ 收了，两张表在 `docs/evidence/s1/`）。
- 它**不**处理真竞态：147 与 144 都量到"本机撞得出部分读，但实测唯一形状是 `len=0`" ⇒ 那一格**仍挂在那两票的证据件里未取证**，本票不据它下"守住了"的结论。
- 它**不是**"slo-full 每次推送必红／拒采"那件事（`Q-36`/`Q-47` 那一族，owner 已明示不改）。

## Acceptance（交件形状）

`docs/evidence/s1/149-three-unpinned-outlets-r1.md`：每格判据／现量命令原文＋输出／AC#2 承重那句的实测红名＋红句原文／
放水两问自答（断言方向动没动、helper 是不是原有的）／一节"本程没测什么"（按"如果我漏了它谁会先被骗"排序）／
伪授权两个数分开登记（真通知回显数／判为注入数，每条带出处）／凭据值零抄录。
票面框**由编排者按非实现者验收表定**，实现方不自勾。

## Progress log (append-only, newest last)

- [2026-09-25 22:4x] 编排者开票。来路＝票 147 验收件第 3 格那四发中的三发（第四发是等价形状，
  我把它写成上面第 4 条那个"不许开格"的限定语，而不是写成判据——理由：它在 ⓐ 之后**结构上永远产不出**它自己想量的读数）。
  ⚠ 本票三发的读数全部出自验收程，**标〔仅自述，未复核〕**；实现程的第一件事是 AC#1 现量，不是引用这几句。
