# 09 — LLM provider seam: C5/C6 core, OpenAI Chat adapter, golden replay, mockllm server

**Status:** in-progress
**Claimed by:** orchestrator -> sub-agent T09-impl
**Last update:** 2026-09-19T13:56:41Z
**Blocked by:** 03-skeleton-runtime-rules
**Parallel slots:** ≤2 sub-agents (A: llm module + OpenAI Chat adapter; B: tools/mockllm server
+ golden harness + compose wiring)
**Spec refs:** SPEC-05 §3, D8, C5, C6, §14.2, D42#4, SPEC-11 §4

## What to build
The `llm` module seam: normalized `LlmProvider` interface, `StreamEvent` union (C6), internal
message representation (C7 incl. Image part), usage accounting, retry/backoff, and the first
adapter (OpenAI Chat Completions with tool_calls). Plus `tools/mockllm` (OpenAI/Anthropic-
compatible mock with golden replay + fault injection) and the golden-SSE test harness that all
later agent tests reuse.

## Key constraints
- C6 event set exactly: TextDelta, ReasoningDelta, ToolCallStart{id,name}, ToolCallArgsDelta,
  ToolCallEnd{id}, Usage{in,out,cached}, Stop{reason ∈ end_turn|max_tokens|tool_use|
  stop_sequence|content_filter|cancelled|error}, Error{class(D37), provider_code, retryable,
  retry_after}, Done.
- C7 content parts: Text | Image{mime, bytes_ref, alt} | ToolUse | ToolResult{id, content[],
  is_error}; bytes_ref never persisted/logged (privacy).
- Adapter maps normalized↔wire; agent core never sees protocol. Errors classified into D37
  classes at the seam.
- Retry: exponential backoff ≤3 for network/5xx/429 (429 honors retry-after); 401/403 → no
  retry, Unconfigured; stream mid-disconnect → keep partial, mark incomplete, unclosed tool
  calls → failed (consumed by 10). Proxy: system proxy (WinHTTP default) + HTTP(S)_PROXY env;
  TLS-intercept errors must distinguish "untrusted cert" from "unreachable" (D42#4).
- Timeout: `[llm] timeout_ms` via context; cancellation propagates (ctx) — no wall-clock math.
- mockllm: `POST /v1/chat/completions`, `/v1/responses`, `/v1/messages`; stream:true SSE with
  tool_calls + usage; golden mode replays `testdata/golden/<name>.sse`; control endpoints
  `/__control/fail_next|latency|truncate|reset`. Runs via `go run` AND compose (port 18080).
  Golden file format shared with unit-test replayer (one format, two runners).

- **Provider presets & catalog primitives (2026-09-19 supplement)**: preset table
  `openai/anthropic/deepseek/qwen/zhipu/moonshot/siliconflow/openrouter/ollama/minimax/mimo/
  stepfun` (protocol + base_url defaults; model entries user-configured or auto-discovered);
  **auto-discovery** via OpenAI-compatible `GET /v1/models` → import with capabilities=unknown;
  **probe primitive interface** on C5 (per-capability test request definitions: fc/vision/
  thinking/audio — implementation in 11, results → `provider_health`, SPEC-02 v2).

## Out of scope
- Anthropic/Responses adapters (11); agent loop (10); probe IMPLEMENTATION (11); quota
  enforcement (44).

## Acceptance criteria
- [ ] Golden replay tests: tool-call assembly, stopReason=max_tokens, mid-stream disconnect,
      cancellation, backoff ladder — all via recorded SSE bytes (no function mocks).
- [ ] mockllm golden mode + unit-test replayer produce byte-identical event streams.
- [ ] Fault-injection tests via control endpoints: 429 (retry-after honored), 5xx ×3 → provider
      error, 401 → no retry, latency injection respected.
- [ ] Proxy tests: HTTP(S)_PROXY honored; untrusted-cert error class distinct from unreachable.
- [ ] Usage events aggregate correctly across a multi-chunk stream.
- [ ] Preset table covers all 12 providers with correct protocol/base_url defaults; auto-discovery
      against mockllm `/v1/models` imports with unknown capabilities; probe primitive contract
      test (each capability has a defined minimal request/response check).

## Progress log (append-only, newest last)
- [2026-09-19T13:56:41Z] agent=orchestrator claimed=T09-impl did=dispatched next=sub-agent works through acceptance criteria
- [2026-09-19T14:28:44Z] agent=T09-impl did=startup+seam-core: read ticket+SPEC-05 sec3+PLAN D8/D42 + ticket-05 config catalog; internal/llm C5 provider.go (LlmProvider/Request/ProviderInfo cache-breakpoint capability bits/protocol registry with ticket-11 pointers), C6 events.go (exact 9-kind event set, StopReason enum, Usage max-merge aggregation, TurnCollector with incomplete-marking + open-tool-call detection), C7 content.go (sealed 4-part union, RedactContent bytes_ref privacy), errors.go (D37 classification: 429 retry-after honor + insufficient_quota->budget, 401/403->auth no-retry, D42#4 tls_untrusted_cert vs connect_failed vs timeout vs stream_disconnected provider codes, no-retry sentinel, Retry-After parser, proxy func env+WinINET systemproxy_windows/other), retry.go (ladder <=3 with terminal-event buffering so swallowed attempts stay invisible to consumer), chain.go (text_chain failover: auth/budget/provider-exhausted/network step to next, failover via OnFailover callback since C6 set is exact, no failover after partial payload), ratelimit.go (rpm/tpm monotonic token buckets + usage reconcile), resolver.go (catalog consumption: roles/text_chain/key-ref resolution/BuildChain), discover.go (/v1/models discovery + capabilities-unknown import), probe.go (fc/vision/thinking/audio cases; audio request DEFERRED: C7 has no audio part, lands with tickets 15/60/61 - response check defined, RunProbe never fakes pass), golden/ (v1 format parse+write + httptest replayer, format mirrored in tools/mockllm); go build+vet internal/llm green next=openaichat adapter
- [2026-09-19T14:52:00Z] agent=T09-impl did=openaichat-adapter: internal/llm/openaichat (adapter.go Stream: POST {base}/chat/completions stream:true, Bearer auth, compat extra_headers, non-2xx body parse -> llm.NewHTTPError classified, transport errors -> ClassifyTransportError incl ctx-cancel-as-cancelled; consume(): SSE line scanner 4MB buffer, data:/[DONE]/comment lines, mid-stream wireError payloads classified, ctx check per line -> Stop{cancelled}+Done, missing-[DONE] = stream_disconnected unless compat.loose after finish_reason; finish_reason map stop->end_turn/length->max_tokens/tool_calls+function_call->tool_use/content_filter/stop_sequence; missing finish_reason tolerated only loose|allow_missing_usage; missing usage errors unless loose|allow_missing_usage) + wire.go (reasoning_content|reasoning union, flat+prompt_tokens_details cached tokens, string-or-number error code, type->status table for mid-stream errors) + toolasm.go (index-keyed reassembly, Start gated on id+name, buffered args flushed after Start, End in wire-index order, missing index only in loose, loose synthesizes call_unnamed_N) + request.go (C7->wire: system TextPart join, user plain-string vs parts-array with image data:URL via Request.ResolveBytes - bytes never logged, assistant text+tool_calls merge, tool role with ToolCallID, is_error prefixes "tool error:", ThinkingIntensity deliberately unmapped for openai-chat documented); provider.go EndpointOptions gained ContextWindow; build+vet green next=mockllm module
- [2026-09-19T15:00:00Z] agent=T09-impl did=mockllm-module: tools/mockllm as SEPARATE stdlib-only go module (go build+vet+test green; nested module excluded from parent ./...): POST /v1/chat/completions (stream SSE chunked role/reasoning/text/tool_calls/finish/usage/[DONE] + non-stream; deterministic synthesis: echo last user text, vision-ok on image_url, audio-ok on input_audio, [think] marker -> reasoning_content, forced|required|auto+[tool] tool call, max_tokens*4char heuristic -> finish_reason=length), POST /v1/responses + POST /v1/messages framework-level SSE dialects (message_start/content_block_delta/message_delta cumulative usage; response.created/output_text.delta/completed), GET /v1/models (discovery target, from -models flag), control endpoints /__control/fail_next{status,times,retry_after,body} latency{ms} truncate{chunks} reset state; golden mode via model="golden/<name>" or X-Wisp-Golden header replaying testdata/golden/<name>.sse section-cursor per name, byte-faithful body serving, parseGolden mirrors internal/llm/golden grammar (documented mirror contract); 8 module tests green (echo assembly, tool/vision/audio/max_tokens/think synthesis, fault queue + retry-after header, truncate cuts before [DONE], golden byte-identity vs fixture + ladder + exhaustion + reset, /v1/models shape, messages+responses smoke); counters mutex-based resettable next=main-module golden unit tests
- [2026-09-19T15:25:00Z] agent=T09-impl did=tests+golden-harness+provider-health+recorder: (1) golden fixtures internal/llm/testdata/golden/*.sse x10 (tool-call fragmented args 2 calls, max-tokens, disconnect, usage-multichunk, backoff-429 retry-after:1 x2 + 200, provider-500 x5, unauthorized 401, missing-usage, missing-finish, long-text @latency 40 x30 chunks); *.sse pinned LF in .gitattributes (byte fidelity); (2) openaichat golden-driven suite ALL PASS: tool-call assembly (2 calls, args fragments by index, Start-before-ArgsDelta contract, End order, usage{21,9,8}, stop=tool_use), max_tokens stop, mid-stream disconnect (network/stream_disconnected + partial kept + incomplete), usage max-merge aggregation (10/7), backoff ladder via recorded 429s (3 requests, >=2s elapsed, retry-after honored verbatim), 5xx x3 exhausted -> provider (4 requests, fast backoff injected), 401 no-retry (1 request, class auth = Unconfigured semantics), compat matrix strict-vs-loose|allow_missing_usage (missing usage / missing finish_reason / no [DONE] / missing tool index), cancellation mid-stream -> Stop{cancelled}+Done clean, proxy env honored (recording proxy), TLS untrusted-cert code tls_untrusted_cert != connect_failed for dead port (D42#4), Info() cache bits implicit/no-breakpoints; (3) seam tests: D37 status table, retry ladder/terminal-buffering/no-retry-sentinel, chain failover order+callback+exhaustion+no-failover-after-payload, rpm/tpm buckets wait+ctx-cancel+usage-reconcile, preset 12-provider pin, discovery import capabilities-unknown, probe contract (4 cases; fc/vision/thinking minimal requests, audio honest deferral); (4) mockllm integration (real subprocess, builds tools/mockllm module): golden byte-identical event streams replayer-vs-mockllm for 4 fixtures, fault injection VIA CONTROL ENDPOINTS (429 retry-after 2s, 5xx x3 -> 4 requests provider error, 401 -> 1 request, latency 120ms respected, truncate -> stream_disconnected+incomplete), /v1/models discovery import unknown caps; (5) provider_health PRIMITIVE (SPEC-02 sec 3 note "票 09 定义原语"): memory schema v1->v2 migration (provider/provider-model composite PK, probe_json, last_probe_at, last_error(+_at), latency_ms_p50, quota_state), SchemaVersionTarget 1->2 with backups bak-0-1+bak-1-2, contract test updated to 9 tables + v2 DDL statement count, DAO primitive (UpsertProviderProbe/RecordProviderError/ProviderHealthRow/ListProviderHealth, D37 enum + quota_state vocab enforced at DAO layer, 3 tests), SPEC-02 sec 3 sql block added byte-identical to ddlV2; (6) cmd/llmrecord recorder CLI: full record path implemented+tested against local server (request->capture->golden v1 write->parse round-trip), no-key failure explicit naming H2 - live provider recording DEFERRED until key (H2, docs/reports); (7) matrix: gofmt clean (my files), go vet ./... clean except pre-existing cmd/wisp sherpa note only under CGO0 (same exclusion as T05/T06 baseline), go test ./... -count=1 ALL PASS (llm 1.4s, llm/golden, openaichat 19.8s incl integration, memory 12.3s, config/observe/ball/etc untouched-green, audio green=T13's), CGO_ENABLED=0 internal+llmrecord vet/test green, mockllm module test green next=closeout+handoff
- [2026-09-19T15:26:00Z] agent=T09-impl did=handoff-to-orchestrator: ticket 09 implementation COMPLETE except explicit DEFER items (below). Acceptance criteria reconciliation: (1) golden replay tests tool-call assembly/max_tokens/mid-stream disconnect/cancellation/backoff ladder ALL PASS via recorded SSE bytes (internal/llm/testdata/golden x10 + openaichat/adapter_test.go), no function mocks; (2) mockllm golden mode + unit replayer byte-identical event streams PASS (TestMockllmGoldenByteIdenticalEvents, 4 fixtures through the same adapter against real mockllm subprocess); (3) fault injection via control endpoints PASS: 429 retry-after honored (>=2s, verbatim sleeps), 5xx x3 exhausted -> provider error after exactly 4 requests, 401 -> 1 request no retry class auth/Unconfigured, latency 120ms respected, truncate -> stream_disconnected + incomplete; (4) proxy PASS: HTTP(S)_PROXY honored via recording proxy, tls_untrusted_cert provider_code distinct from connect_failed (D42#4); (5) usage aggregation PASS (max-merge across multi-chunk, Anthropic-cumulative-safe); (6) preset table 12 providers pinned, /v1/models auto-discovery imports capabilities-unknown, probe primitive contract test (fc/vision/thinking minimal request+check defined, audio defined-but-deferred). DELIVERABLES: internal/llm seam (C5/C6/C7, D37 classification, retry/chain/ratelimit, resolver, discovery, probe) + internal/llm/golden (format+replayer+writer) + internal/llm/openaichat (adapter) + internal/memory schema v2 provider_health DAO primitive (SPEC-02 sql block synced, contract test 9 tables v2) + tools/mockllm (separate stdlib module: chat/responses/messages, golden replay, /v1/models, /__control/fail_next|latency|truncate|reset|state, 8 tests) + cmd/llmrecord (recorder, record path tested). Commits: fe87e12 f27928e 0839407 3b7bf24 (+ .gitattributes LF pin, chore) all pushed origin+cnb. Matrix: go test ./... -count=1 ALL GREEN (audio green = T13's work untouched); CGO_ENABLED=0: internal/... + cmd/llmrecord vet/test green, cmd/wisp sherpa cgo gate = pre-existing documented exclusion; gofmt clean on all files I touched; mockllm module vet+test green. DEFER (registered, non-blocking): (a) live provider golden recording - blocked by H2 (LLM API Key, docs/reports/pending-and-issues.md); harness ready, llmrecord ready, re-run `llmrecord` when key arrives; (b) probe IMPLEMENTATION + provider_health real writes = ticket 11; (c) audio probe REQUEST definition: C7 has no audio content part (SPEC-05 sec 3.3 pins 4 parts) - response check defined, request lands with tickets 15/60/61 owning that decision; RunProbe(audio) returns honest not-implementable, never a fake pass; (d) compose wiring (port 18080) = docker/ owned by later ticket per orchestrator parallel protection; mockllm default port already 18080. DEVIATIONS (design choices needing no spec change): failover signaled via ChainRunner.OnFailover callback (C6 event set is contract-exact, no synthetic events; ball/panel wiring = tickets 10/33); no-retry-sentinel refines observe class policy for persistent TLS-intercept errors (documented in errors.go); ThinkingIntensity intentionally unmapped in openai-chat (no portable wire field, private params cause 400s) - anthropic adapter (11) consumes it; text_chain no failover after partial payload (would duplicate a half-delivered response). Ticket NOT marked done (orchestrator decides).
- [2026-09-19T16:00:00Z] agent=T09-impl did=t09-adv-fixes: MAJOR-1 FIXED (commit dd698b8): byte-identity comparator rewritten - sameEvent恒真 deleted, replaced by (a) RAW-BYTE axis: rawGolden (was dead code) now asserts mockllm serves the fixture section bytes verbatim before any event comparison (statement about identical input, cursor rewound via /__control/reset between the two mockllm requests), (b) per-event FULL-JSON comparison incl. TextDelta text (Err compared by value not pointer identity; SSE line scanner is TCP-chunk-insensitive so both runners must produce identical events), (c) redundant assembled-text axis (textOf concatenation). LIVENESS PROVEN per adjudication protocol: injected one-char divergences between the runners (raw+X and first TextDelta+X) -> test RED on all 4 fixtures with exact diff shown (raw axis: "served bytes differ" x4; event axis: "event 0 differs" showing Let me check. vs Let me check.X); note: tampering the FIXTURE FILE itself leaves the test green BY DESIGN (both runners read the same file, runner-identity invariant holds) which is why liveness was proven by injecting runner divergence instead; injections fully removed, 4 fixtures re-run GREEN. MINOR-1: TestMigrateSeededV1DatabaseToV2 - genuine v1 db (withSchemaTarget(1)) seeded via regular write path (profile+memory rows, asserted provider_health absent at v1) -> reopen at production target -> 1->2 step runs (version 2, provider_health usable, seeds intact, bak-0-1+bak-1-2 exactly). MINOR-2: both 429 assertions unified to measured口径 >=1.9s with rationale comment (hints sum 2s; non-honoring backoff would return ~10ms at 5ms base). MINOR-3: cancellation test now asserts ZERO EvError events in the stream (clean end, not a failure). MINOR-4: DEFER(d) compose wiring target pinned = TICKET 08 (owns CI/compose incl docker/compose.test.yml mock-llm service stub). Matrix: my packages all green (llm 1.46s, llm/golden, openaichat 19.9s incl integration, memory 12.4s, llmrecord); internal/models failures + gofmt noise = T14 in-flight uncommitted WIP (not touched per parallel protection); go.mod/go.sum modifications = T14's (not committed by me). Commit dd698b8 (explicit paths: openaichat/adapter_test.go, openaichat/mockllm_integ_test.go, memory/migrate_v1seed_test.go) pushed origin (TLS retry consumed 1 attempt) + cnb. Status NOT changed (orchestrator re-reviews).
