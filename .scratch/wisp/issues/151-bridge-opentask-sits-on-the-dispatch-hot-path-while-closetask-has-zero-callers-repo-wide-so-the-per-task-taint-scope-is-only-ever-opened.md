# 151 — `Bridge.OpenTask` 在派发热路径上、`Bridge.CloseTask` **全仓零调用者（连测试都没有）**：per-task 污点 scope 今天**只开不关**

- Status: ready-for-agent
- 来源：票 138 对抗验收件第 4 节"本程造出的新读数"（编排者 09-26 00:5x 已独立复核，见下面第 1 条）
- 关联：`internal/tools/provenance.go:55-57` 那一族 **C25** 自点名账（12／19／21／22／26）· 票 138（同因"缺任务级载体"，但**这一发今天可达**）
- 地界：`internal/tools/bridge.go`（`OpenTask`/`CloseTask` 那一对）＋ 装配它的组合根（`cmd/wisp/run.go` 的 `agent.New`／任务结束那条路，⚠ **与 149 那程的落点同包不同文件，派单前先看工作树**）＋ 本票自己的证据件

## 现量的形状（编排者 00:5x 现跑，**下一程一律现量，别抄这里的数**）

1. `grep -rn 'CloseTask' --include='*.go' internal cmd` ⇒ **只有两行命中，都是它自己**：`bridge.go:646` 的函数声明＋`bridge.go:642` 的注释。
   **调用者 0 枚——测试里也没有**（这一条比"生产 0 枚"更硬：没有任何一处今天试过它）。
2. `grep -rn 'OpenTask' --include='*.go' internal cmd | grep -v _test` ⇒ `bridge.go:559` 在**派发热路径**上调 `b.OpenTask(dec.TaskID)`，其余两行是声明与注释。
3. `CloseTask` 自己的注释写着 "**The composition root defers this on…**"——即**设计期望组合根去 `defer` 它**，而组合根今天没有。
4. ⚠ **`grep -rl 'CloseTask' .scratch/wisp/issues/` ⇒ 0 枚**：这缺口**此前不在任何一枚票上**（不是重复开票）。

⇒ 形状一句话：**每开一轮任务就往污点账上挂一枚 scope，谁都不摘。**

## 为什么这不是"少了一行 defer"

两件事本票要分开判，别混着修：
- **有没有资源／语义在长**：任务结束后那枚 scope 留在表里 ⇒ 长跑的常驻腿会**一路加不减**。它的实际后果边界（内存？判定？同名复用？）**验收程与实现程都没量到就不许说"只是难看"**，也不许说"泄漏很大"——**两侧都不许凭感觉报数**（这条我特意写死，因为我自己现在就没量）。
- **有没有判定被污染**：`CloseTask` 的语义若是"这一轮的污点结论作废"，那**不关**就意味着**下一轮可能读到上一轮留下的污点状态**。⇒ 那一发如果成立，属**跨任务串味**，比内存优先级高得多。
  ⚠ **这一问就是本票的硬核心（AC#2），不许用"看起来不严重"收。**

## AC

- [ ] **AC#1 先量"不关"今天到底改变了什么**：给出可复算的读数——同一条 `Bridge` 上连开 N 轮（N 取 2 与一个更脏的值）之后，
      scope 表的大小、以及**第 N+1 轮的判定是否读到第 N 轮留下的东西**。判不了就**停手上报**（D22 闸门③），不许按偏好填。
- [ ] **AC#2 会响的检（硬核心）**：造一发**只有"上一轮的污点痕迹渗进下一轮判定"才响**的断言（或反过来：一发"表只增不减"的量化断言）。
      承重按本仓操作定义答一句：**摘掉本票选定的一味，是否存在一发变异从此打不红？**
      ⚠ **恒真判据是一类新假绿**：先答"这一发在**未修码**上响不响"。
      ⚠ **如果 AC#1 量到"今天既不渗、也不长"**（例如每轮 key 唯一、表随进程寿命无害），那 AC#2 就**结构上产不出它自己想量的读数**——
      那种情况的正解是**本票作废并登记为什么**（外加把"该不该关"退给 `Q-43` 那一族），**不许**硬开一道永不响的格凑数。
- [ ] **AC#3 修法只在"谁拥有关闭"这一层**：组合根 `defer`／`Bridge` 内部自管／任务生命周期事件——**三选一先裁再写**，
      裁不动就停手上报。⚠ **不许**顺手把 `OpenTask` 也改了来"让两头对称"（那是扩大射程）。
- [ ] **AC#4 契约轴（零字节）**：`internal/risk/**`、`internal/panel/**`、`internal/agent/approval/**`、`tools/d22scan/**`、
      `thresholds.go`、任何 golden、`allowlist.txt`、`scripts/slo-check.ps1`、`docs/PLAN.md`、`docs/specs/**`、
      `frontend/**`、`design/**`、`cmd/wisp/slo_windows.go` 与 `cmd/wisp/slo_report_144_windows_test.go`（**票 149 那族的地界**）。
- [ ] **AC#5 门禁（逐包单跑）**：`go test ./internal/tools/`（以及被改到的每一包各一次）改前改后，四数之外**名册两向 `comm`**；
      `go vet` 空；`gofumpt`（**版本自己 `--version` 现读并贴出**；`gofmt` 是另一把尺）空；`sh scripts/d22scan.sh` rc=0 且各作用域 `examined N` 非零。
      ⚠ 判红绿只认 `--- FAIL:`，且**不许用裸 `grep -c '--- FAIL'` 数枚数**（本仓实测：日志汇总行会造出假命中）；⚠ 一枚 panic 会吞掉同包其余几十条读数。

## 落点与凭据规矩（编排者加）

原始读数一律落 **`.scratch/wisp/probes/151/`** 并随 commit 入库（`A264③` 定的落点；票 138 那程把读数只留在 `/tmp`，是那条规矩生效后第一枚没照做的——**别重复它**）。

## Acceptance（交件形状）

`docs/evidence/s1/151-task-scope-never-closed-r1.md`：每格判据／现量命令原文＋输出／AC#2 承重那句的红名＋红句原文／
放水两问自答（断言方向动没动、helper 是不是原有的）／一节"本程没测什么"（按"漏了它谁会先被骗"排序）／
伪授权两个数分开登记（真通知回显数／判为注入数，每条带出处）／凭据值零抄录。
票面框**由编排者按非实现者验收表定**，实现方不自勾。

## Progress log (append-only, newest last)

- [2026-09-26 00:5x +08] 编排者开票。四条现量全部自己重跑（`CloseTask` 调用者 0 含测试／`OpenTask` 在 `bridge.go:559` 热路径／
  注释自称"组合根 defer 它"／票池里此前**没有任何一枚票提到 `CloseTask`**）。
  来源＝138 验收程造出的新读数——**它的这条我按既有条独立重走过一遍，不是因为它是验收程就算已核。**
