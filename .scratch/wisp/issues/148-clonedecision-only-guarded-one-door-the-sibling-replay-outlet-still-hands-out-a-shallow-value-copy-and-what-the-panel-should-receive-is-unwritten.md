# 148 — `cloneDecision` 只护住了 `LiveApprovals` 那一扇门：同族第二出口 `replay` 里仍是 `d := it.Dec` 直接按值交出去（浅拷贝原形未变）＋"要不要继续往外送 `Params`"这两问都卡在人工批准上

- Status: **blocked（两枚前置都是人工批准，不许实现方自决）** —— 见下面「批准清单」，回两个字母即可解锁
- 来源：票 146 第二轮验收件 `docs/evidence/s1/146-liveapprovals-r1-accept-r2.md` §它给编排者的三件裁定建议（**本票是第 ②③ 两件，第 ① 件"再往下一层"按建议只记台账、不单独开票**，账在 `A261`）
- 关联：票 146（`cloneDecision` 的来路）、票 144/147（同为"错误/字段说了它没做到的话"那一族）、`Q-51`（那一族的产权问题）
- ⚠ **本票两处"现量"是引两轮验收件的读数**，档位＝〔日志＋归档，抽验〕（编排者本轮**只**复核了符号存在性，见下条命令）。
  **下一程一律现量**，别抄这里的行号与枚数。

## 现量的形状

- **同族第二出口仍在（本条编排者 21:2x 现跑复算，档位＝〔本轮现跑〕）**：
  `grep -n 'func.*[Rr]eplay' internal/agent/approval/queue.go` → **`:478 func (q *Queue) replay(corr string) (tools.Decision, string, error)`**；
  其内 **`:493 d := it.Dec`** —— **没有走 `cloneDecision`**。
  ⇒ 另跑一条 `grep -rn ':= it\.Dec' --include='*.go' internal cmd | grep -v _test.go` → **全仓非测试命中恰好 1 处，就是 `:493`**。
  ⇒ 也就是：票 146 把 `LiveApprovals()` 这一扇门后的 8 枚引用槽位真拷了一层，**而另一扇门（`replay`）把同一个"按值拷贝一个含引用的结构"的形状原样留着**。
  ⇒ 那一族缺陷的名字在票 146 里已经定过：**注释许诺 "copies"、实现给的是"浅一层"**。
- **顺手量到第三处、形状不同（同属本票）**：`queue.go:453-456` 那份投影是**逐字段挑着拷**的——
  `Tool`／`LevelString()`／`Reason` 各取标量，而**只有 `Paths` 做了一次 `append([]string(nil), …)` 真拷**。
  ⇒ **8 枚引用槽位里它只护了 1 枚**（`Params`／`Args`／`RulesHit`／`Capabilities` 那几枚根本没进这份投影 ⇒ 那份投影今天不携带它们，
  所以**这不是漏拷、是范围不同**）⇒ 判"这份要不要一起收"归 `AC#3` 与 Ⓑ 一起定，**别按"第三枚 bug"处理**。
- **第二个问题不是拷贝深浅、是"该不该送"**：`LiveApprovals()` 的返回值今天带着 `Params`（**完整工具参数**）往外走，
  今天唯一的读侧是 `cmd/wisp/panel_pump.go`（只读、且落的是有界摘要）。
  ⇒ 但票 146 的 `AC#4` 那句话已经把边界钉在"**这枚函数不能变成入站路由**"；
  而**往外送的字段范围**今天没有任何一份冻结文本明确说过"该送哪些"（这一格是空的，**别读成"已批准全送"**）。

## 批准清单（这就是本票 blocked 的全部原因，两问彼此独立）

- **Ⓐ 放开 `internal/agent/approval/queue.go` 的禁改面**（只这一枚函数的这一行）⇒ 才谈得上把 `replay` 也换成 `cloneDecision`。
  ⚠ 它是审批队列本体，票 146 全程零字节。**批准＝回「批 148 Ⓐ」**；不批也行——那本票只剩 Ⓑ＋`AC#3`。
- **Ⓑ 批准改写票 146 `AC#2` 那句"往外送的字段范围"的口径**（要么具名收窄成"面板只需要的那几枚字段"、要么具名写"全字段照送＋理由"）。
  ⚠ 这一支碰的是**面板载荷的契约面**，`AGENTS §0.2`（改契约＝人工批准）。**批准＝回「批 148 Ⓑ」**。
- ⛔ **这两支都不许实现方自决**，也不许由编排者替 owner 推出来（`Q-49` 那一次我替批准者加了条件、被自己收回；`Q-52` 那一次我摆错了时刻、已撤回）。

## AC

- [ ] **AC#1（依赖 Ⓐ）把同族第二出口一起收掉**：`replay` 那一行换成走 `cloneDecision`（或等价的一份真拷贝），
  **并配一枚会响的检**——照票 146 `AC#2` 的规格做：只有"就地改掉那一出口的返回值里的引用槽位"才能触发，
  断言队列里那条原始记录没跟着变；承重那句按本仓操作定义答（**摘掉修法这一发是否从绿变红**）。
  ⚠ 不许只改实现不配检（那是票 146 第一轮 ⓑ 被退的同一形状）。
- [ ] **AC#2（不依赖任何批准，本票今天就能做）把票 146 `AC#3` 那支"从未字面复算过"的补上**：
  让 WorkPeak 档的 subject **真的跑起工具调用**（即 `LiveApprovals` 在那 5 秒里被调用 ≥1 次，**先证明这一点再取数**），
  然后取"多拷那 8 枚槽位"的前后读数。⚠ **阈值与 golden 一字节不许动**；数字没动写"未观察到差异"、**不写"无代价"**。
  ⇒ 触发条件具名在票 146 的 `>` 注里；这一格结掉之前，**票 146 `AC#3` 那句"字面那支从未复算"不许被抹掉**。
- [ ] **AC#3（依赖 Ⓑ）把"往外送什么"写成一个有边界的句子**：范围由 owner 批的那个方向定，
  实现方只负责把它变成**可核的**东西——要么是一份字段白名单＋一枚会红的对账，
  要么是明写"全字段照送"＋一句为什么，**不许留在"今天没人规定"这种形状**。
  ⚠ 若走白名单：**`Params` 再往下一层仍共用**这件事（value 里的 `[]any`／嵌套 map，`reflect.DeepEqual` 照不到，
  第二轮验收件在**健康锚点**上量到"写前写后都 true、队列记录真被改掉、仓内零条用例红"）
  **必须一起处置**——把它留成"已知瞎"，等于把票 146 那扇门重新打开一条缝。
- [ ] **AC#4 契约轴**：`internal/risk/**`、`thresholds.go`、golden、`allowlist.txt`、`tools/d22scan/**`、
  `scripts/slo-check.ps1`、`docs/PLAN.md`、`docs/specs/**`、`frontend/**`、`design/**`、
  `internal/panel/l2_grant_boundary_test.go` **一律零字节**；
  **未被批准前 `queue.go`／`approval.go` 也零字节**（批准只放开 Ⓐ 那一枚函数那一行，不是放开整枚文件）。
- [ ] **AC#5 门禁（一律逐包单跑）**：`go test ./internal/agent/approval/` 改前改后各一次＋**名册两向差集**；
  `gofumpt -l . tools/d22scan tools/mockllm` 空（v0.12.0、与 CI 同版）；`go vet` 空；`sh scripts/d22scan.sh` rc=0 且 `examined N` 全非零。

## 这一枚票**不**解决的事（登记，别顺手扩大）

- 它**不**把 `LiveApprovals` 变成入站路由（票 146 `AC#4` 那句钉着的边界；那要重新走契约批准，`Q-51` 那一族）。
- 它**不**重开票 146 已结的五格。
- 它**不**处理 `Q-51`（两侧必须同批、而两侧归属不同作者）——那一格仍等 owner。
- 它**不**替 owner 决定"面板该显示工具参数的哪几个字段"——那是 Ⓑ 的内容，不是本票的实现细节。

## Progress log (append-only, newest last)

- [2026-09-25 21:2x] 编排者开票并**立为 blocked**：两枚前置都是人工批准（Ⓐ 放开 `queue.go` 那一行、Ⓑ 改写面板载荷口径），
  按 `AGENTS §0.2` 不由我裁。`AC#2` **今天就可做**（不需要任何批准），但它的落点与票 144/147 同在 `cmd/wisp/` 那条链路上，
  ⇒ **排程：等 147 交件之后一起派，别三枚程同抢 `cmd/wisp` 与 `internal/agent/approval`**。
  批准清单已同步进台账 `A261` 的 Q 表（`Q-53`／`Q-54`），owner 回两句话即可解锁。
