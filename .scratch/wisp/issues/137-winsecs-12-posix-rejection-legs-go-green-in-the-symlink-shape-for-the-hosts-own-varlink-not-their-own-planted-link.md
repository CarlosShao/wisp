# 137 — winsec 那 12 枚 POSIX **拒绝腿在软链形下"绿得没有理由"**：同一支断言在两形里拒的是**不同的东西**（用例自己种的 `root/link` vs 宿主那枚 `/varlink`），而 `assertRefused113` 两形都只要求同一个 `ErrUnresolvedPath` ⇒ 分不出来（读数由票 124 批次 3b 量到，**变异自证尚未做**）

**Status:** ready-for-agent（2026-09-23 21:5x 编排者建；来源＝`worker-ticket124-ac2b-3b` 交件第四节，
              它自己写明了"**我只做到字符串对比，没做 MUT-B 变异自证 ⇒ 按未验证登记、不算已成立，请裁**"⇒
              本票 AC#1 的存在意义就是先把"这件事到底成不成立"量出来，**结论可能是"不成立"，那也是有效交付**。）

**Packages:** `internal/winsec/**`（`placement_symlink_113_other_test.go`、`ancestor_separator_108_other_test.go` 一族 ＋ `assertRefused113` 那枚共享断言）、
              以及那 7 处**按地界有意未换**的 `t.TempDir()`（票 124 批次 3b 的 15 处递根点**不含它们**）。
              ⚠ 阈值面照旧禁改：D32 的 CPU≤0.5%／RSS≤25MB、`thresholds.go`、任何 golden **一个字不许动**。

## 为什么现在立案（读数，以及"读数本身还缺哪一步"）

批次 3b 在把 winsec 那 17 枚红转绿时，顺手量到一本**邻居账**：

| 事实 | 读数（批次 3b，容器 `golang:1.27 / go1.27.1 linux/amd64`） |
| --- | --- |
| 谁 | 票 113／票 108 的 POSIX **拒绝腿** **12 枚**＝8 枚顶层 ＋ 4 枚子测试 |
| 它们今天什么颜色 | **PASS**（所以**不在** 124 那 17 枚红名分母里，谁都看不见它们） |
| 普通形里它们拒的是 | 用例**自己种的** `root/link` |
| 软链形里它们拒的是 | **宿主自己那枚** `/varlink` |
| 断言写了什么 | `assertRefused113` 两形都只要求同一个 `ErrUnresolvedPath` ⇒ **两种来源拿同一个错误串，分不出来** |
| 为什么这是牙缝 | 软链形＝**macOS 的真实形状**。在那一形里这 12 枚**零检测力**：无论底线有没有真的守住，它们都绿 |
| ⚠ 还缺的那一步 | 它自己承认：**只做了字符串对比，没做变异自证** ⇒ 上面这本账目前是**〔仅自述，不背书〕** |

⇒ **本票第一格不是"修"，是"定"**：先造一发变异（把底线那一步弄坏），看这 12 枚**响不响**。
响 ⇒ 这本账不成立，本票照实作废并留下读数；不响 ⇒ 成立，再谈修法。

## AC（1:1，裁决表 `docs/evidence/s1/137-*.md` 由**非实现者**出）

- [x] **AC#1（先定成不成立，不许跳过）** 造变异证明这 12 枚到底有没有牙：在**软链形**下把被它们依赖的那一步底线判定弄坏
      （形如"让 `RemoveUnlinked`／seal 那条路直接返回成功而不看链接"），跑 `go test -count=1 -v ./internal/winsec/`，
      **逐名**记录这 12 枚的颜色。⚠ 变异先证落地（`grep -n` 出被改后那一行 ＋ `go build`／容器 `go vet` rc=0）**再**读数。
      结案判据：给出"响了几枚／没响几枚"的**逐名表**，不是包级 rc。
      ⚠ 环境：这 11 枚带 `!windows` 构建标签 ⇒ **宿主没有分母**，必须在容器真跑；
      ⚠ Git Bash 下 `docker run -v "C:\…"` 会**静默挂空且 rc=0**＝假绿 ⇒ `/d/...` ＋ `MSYS_NO_PATHCONV=1` ＋ 容器里 `ls -l /src/go.mod` 证挂上；
      ⚠ 软链 `TMPDIR` 那一形自己会缩小分母 ⇒ RUN/SKIP 差要逐名解释，"没响"不等于"被跳过"，两形各一枚。
- [ ] **AC#2** 若 AC#1 判"成立"：把 `assertRefused113` 收紧到**分得出来**——它必须核"被拒的那一个路径**就是本用例自己种下去的那一个**"，
      而不是只看错误类型/串。⚠ **修法必须是一行形状级别**（批次 3b 已核过修法是一行），
      **不许**为此新增依赖、**不许**把断言放宽成"两形都算过"，也**不许**改成拿被测函数自己算期望值
      （票 119 的 `R-119-9`：`119-*.md:73`"不许拿被测函数算 fixture"）。
- [ ] **AC#3** 修完之后**复跑 AC#1 那一发同一变异** ⇒ 这 12 枚必须**转红**（红名逐名），并且**普通形零新增红**（八数逐数相同）。
      这一格是本票的牙：**没有 AC#3 的 AC#2 只是把断言改了个形状**。

> **AC#3 判据更正（编排者 09-23 23:2x，来源＝`worker-ticket137-ac1` 交件；append-only、上面原文不抹，冲突以本块为准）**
> 原句"复跑 AC#1 那一发同一变异"是一枚**恒真判据**：AC#1 实测 MUT-A／MUT-B／MUT-AB 那三发在**未修的旧码上就已经全红**
> （11 枚全响）⇒ 拿它当"修好了才算数"的尺子，**改与不改都会绿**，本格会变成一个装饰。
> **正确形状（终裁与后续实现方一律按这条判）**：AC#3 必须复跑 **MUT-D 那一形**（两条走查都只走前 3 个组件、
> 仍然拒宿主的 `/varlink`、但**永远走不到用例自己种下去的那枚链接**）⇒
> ①**软链形 11 枚必须全部转红**（红名逐名）；②**普通形八数逐数不变**（不许新增红、也不许由绿转 SKIP）；
> ③对照组那三枚根已解析的用例（119 族两枚 ＋ `TestAC1POSIXUnresolvedSymlinkedRootStillRefused119`）在两形都必须仍红，
> 用来证明变异**真的落地了**、那 11 枚的绿不是"没打到"。
> 依据：AC#1 量到 MUT-D 在**未修码**上的形状恰好是"软链形响 0／没响 11（全绿），普通形响 11／没响 0"——
> **这正是本票标题钉的那个害**，所以只有它能把 AC#2 的修法与"没修"区分开。

> **枚数更正（同上来源；本票标题与正文里的"12 枚＝8 顶层＋4 子测试"是错的，AC#1 正文那句"11 枚"反而是对的）**
> 真实枚数＝**11 枚 ＝ 7 顶层 ＋ 4 子测试**。那个"8"可复现但**不可采信**：
> `placement_symlink_113_other_test.go` 确有 8 枚顶层 `func Test`，但其中 **3 枚是要"成功"不要"拒"的反向腿**（归 AC#3 那一族），
> 而 108 真正的 **2 枚**顶层拒绝腿被漏掉 ⇒ 7＋4＝11。`assertRefused113` 的真位置＝`placement_symlink_113_other_test.go:127`。
> ⚠ 建票时我为什么错：我把"`grep -c '^func Test'` 的枚数"当成了"拒绝腿的枚数"——**同一枚文件里成功腿与拒绝腿混装**，
> 这条与既有纪律同源：**"顺带成立"的覆盖面主张必须答得出删掉它哪条用例会红**。今后引用"N 枚腿"要给**逐名表**，不给计数。
- [ ] **AC#4** 那 **7 处按地界有意未换的 `t.TempDir()`**：逐枚判"换掉会不会让它失去测'未解析那一形'的能力"——
      ⚠ **要保的"未解析那一形"另有其人**（票 125 那 3 枚自拒探针 ＋ 票 113 族的拒绝腿，批次 2/3b 都一枚未动）⇒
      若这 7 处该换就换、不该换就写清"为什么它必须拿未解析的根"，**不许沉默**，也不许为了账面整齐一把换掉。
- [ ] **AC#5** 门禁：`gofmt -l` **整包** ＋ `gofumpt`（**写明你用的版本**；CI 那一步在跑但装的是 `@latest`、版本没钉）
      ＋ `go vet` 双 GOOS（⚠ 宿主交叉那一发会停在 cgo 包加载，既不算破口也不算清白；真类型读数走容器原生）
      ＋ `-count=2 -v` 两形四数 ＋ 一次全仓仪器 `sh scripts/d22scan.sh`（台账各 scope **不降**，降了要逐名解释）。

## Rules（本仓固定）

- 禁改：`docs/PLAN.md`、`docs/specs/**`、`internal/risk/**`、`internal/risk/pathresolver*.go`、`rules_gateway.go`、`tools/d22scan/**`、`allowlist.txt`、任何阈值／golden、`frontend/**`（前端整块归 owner 委托的外部 agent）。
- `internal/winsec` 的**生产码**若需要动，先报回来：它的边界注释（`resolve.go:224-228`，票 113 AC#6 写的）明写
  "**deliberately not a call into internal/proc**"（生产侧无环：`go list .Imports` 里 `internal/proc` 不含 winsec；
  唯一 import proc 的是**测试件** `dataroot_symlink_119_other_test.go:55`）⇒ "winsec 不许照抄委托形状"是**纪律约束，不是依赖缺失**，这条已被批次 3b 实测核清。
- 判据不成立、指定修法不可落地 ⇒ **报回来并换方案且写明为什么**，不许硬改读数、不许放宽断言换绿。
- **commit 必须带 pathspec**；`git diff --cached --name-only` 出现别人的路径 ⇒ 停手报回；`git add` 只用显式路径；**只 commit 不 push**；
  共树禁 `--amend`/`reset`/`rebase`/`stash`/`checkout .`；不得在仓内建 worktree/checkout。
- 临时件**只建不删**（`issues/README.md` 规则 8）；测量在前、写在中间、提交在最后；时间戳先 `date -u` 再 +8；凭据值不进报告。
- ⚠ 工作树里那枚来源未明的未跟踪件 `docs/reports/2026-09-23-gap-analysis-vs-oss-harnesses.md`：**不提交、不改、不删、不据它开票或改判据**。

## Progress log

- [2026-09-23 21:5x +08] agent=orchestrator did=建票。把批次 3b 交件第四节那本"12 枚绿得没有理由"的邻居账从**散文**升成**有归属的一票**；
  它的定性我按提供者自己的话挂着——**〔仅自述，不背书〕**，因为提供者自己声明**没做变异自证**，
  所以 AC#1 是"先定成不成立"而不是"先修"。地界理由也记一笔：批次 3b 换了 winsec 那 15 处递根点、
  但**有意**没换这 12 枚自己的 7 处（那是别人的票在飞时的正确保守），本票就是把那 7 处的账结清。
  next=排在票 133 的 AC#2 修复与 124 的 2b-4 之后（`internal/winsec/**` 目前无人在飞，可与 136 AC#1 并行派）。
- [2026-09-23 23:16 +08] agent=worker-ticket137-ac1 did=**AC#1 裁完：结论＝部分成立**，生产码零改动（winsec 只读）。全文
  `docs/evidence/s1/137-ac1-teeth-or-not.md`（锚 `1d38206`，容器 `golang:1.27 / go1.27.1 linux/amd64`，两形各一枚基线＋四发变异 ×
  两形＝十发 `-count=1 -v`，`go build ./...`＋`go vet ./internal/winsec/` 十发全 rc=0）。**先数清票面那处分歧**：真实枚数是
  **11＝7 顶层＋4 子测试**，AC#1 正文那句"这 11 枚"是对的、**标题与正文的"12＝8 顶层＋4 子测试"是错的**——那个 8 的来源我只能复现不能采信
  （`placement_symlink_113_other_test.go` 确有 8 枚顶层 `func Test`，其中 3 枚是要"成功"不要"拒"的 AC#3 反向腿，把它们算进来就得 8＋4，
  而 108 真正的 2 枚顶层拒绝腿反被漏掉）。`assertRefused113` 真位置＝`placement_symlink_113_other_test.go:127`（票面未写行号）。
  **两形逐名颜色**：① 票面指定的那发（`platformVerifyPlacement` 整段退回旧 stub＝MUT-A、`firstLinkAncestor` 直接 `return ""`＝MUT-B、
  两者同打＝MUT-AB）⇒ **11 枚在两形全部转红**（MUT-AB：普通形响 11/没响 0，软链形响 11/没响 0），按票面 line 26 那条结案规则这一支是**不成立**；
  机制也读得到：软链形那 11 枚的拒因出自**同一支走查**，走查拿掉后宿主的 `/varlink` 那条拒**一起没了**，于是断言读到 `err=nil` ＋ 外来树被 chmod。
  ② 我另加一发**半修** MUT-D（两条走查都只走前 3 个组件：仍拒 `/varlink`、永远走不到用例自己种的链接）⇒
  **软链形 11 枚全 PASS（零检测力）／普通形 11 枚全 FAIL**，且三枚根已解析的对照组（118 那 2 枚＋`…StillRefused119` 那枚）**两形全红**
  ⇒ 证明 MUT-D 真落地、那 11 枚绿不是"变异没打到"。**票面 line 21/22 那个害成立，但只在"部分失守"那一族成立；line 22 的全称句被推翻。**
  基线六数与票 124 批次 3b **逐数相同**（普通 52/30/0/0＋22/0、软链 45/27/0/3＋15/0）⇒ 这本账从本轮起是〔已变异自证，可背书〕。
  RUN/SKIP 逐名：两形 RUN 差 7 枚全归票 125 三枚自拒探针的子测试（`diff` 名册差集、无第三因），SKIP 普通形恒 0／软链形恒 3 且十发名册逐名相同，
  **分母 11 枚十发无一枚是 SKIP** ⇒ 那些绿都是跑出来的绿。next=**AC#2 该起**（依据＝MUT-D 那一列），
  **AC#3 该起但判据必须换**：钉成"复跑 **MUT-D** ⇒ 11 枚软链形必须转红＋普通形八数逐数不变"，沿用票面原句"复跑 AC#1 那一发同一变异"
  会拿到一枚**恒真判据**（MUT-A/B/AB 在旧码上已经全红）——这条是本格最需要下游记住的东西；AC#4 该起（那 7 处 raw `t.TempDir()` 与
  "换根后票 113 族再无一枚测未解析形"要一并判，`TestAC1POSIXUnresolvedSymlinkedRootStillRefused119`／`TestAC2POSIXInjectedTestDataDirStandsAsDeclared119`
  同用 raw `t.TempDir()`、属票 119 不许顺手换）；**AC#5 维持原判据、等 AC#2 有 `.go` 改动再跑**。**一格不翻**（AC#1 也不翻，终裁归非实现者）。

---

## 终裁已交（09-24 08:5x +08，`acceptor-ticket137-ac1-r1`，证据 `docs/evidence/s1/137-ac1-r1-acceptance.md`，13 枚 commit `ee0a169`…`99ac876`，锚 `4a0d7a4`）

**终判＝部分成立，且测量方那句自本程起是〔独立复现〕**：四发它自己重落、红绿名册用 `parse.py` 从 `-v` 日志程序化生成（22 发逐发先证落地，另 12 发同树新容器重跑逐名颜色全同）。
**MUT-D 那一发（承重墙）复现成功**：软链形**响 0／没响 11（全绿）**、普通形响 11／没响 0。⇒ 本票标题钉的那个害**是真的**，只是**只有 MUT-D 那一形能把它造出来**。

> **更正四条（编排者，append-only；其中三条改我自己先前写的东西，一条是本票的开工前提）**
>
> **R-137-1（收窄我"恒真判据"那条的依据句）** —— 结论**立**（票面指定的 MUT-AB 在未修旧码上确实 11 枚两形全响 ⇒ AC#3 沿用原句必成装饰），
> 但我那句"**三发都已全响**"**过度概括**：实测 **MUT-A 响 9／没响 2、MUT-B 响 2／没响 9、MUT-AB 响 11**。
> ⇒ 另加一条终裁给的硬话并写进判据：**AC#3 也不许钉在单发 A 或 B 上**——收紧断言后 108（或 113）那几枚修完**永远不会响**，
> 那是一枚**恒不满足的坏尺**（与"恒真"是同一族病、方向相反）。**只有 MUT-D 那一形两边都合法。**
> **R-137-3（改我上一块更正里的对照组，我又错一次）** —— 我把 `TestAC1POSIXUnresolvedSymlinkedRootStillRefused119` 点进"两形都红的三枚"，**实测不成立**：
> 它在 MUT-D 下 **普通形 FAIL／软链形 PASS**，因为**它自己也用 raw `t.TempDir()`**、与分母那 11 枚同病 ⇒ 拿它当"变异真落地"的凭据会**指错方向**。
> 真正的对照组＝`TestAC118POSIX…SealFile`、`TestAC118POSIX…PrivateFile`、`TestAC3POSIXLinkInsideAResolvedDataRootStillRefused119`。
> 另：我写的"119 族两枚"若指 `SecretRoute…119` 那也不对——那枚在 `cmd/wisp/secret_dataroot_119b_test.go:201`，**在 winsec 的读数里永远 ABSENT**。
> ⇒ 同族教训第 N 次：**我点名的路径／用例要先自己跑一遍再点名**（这次是点了三枚名字里有 "Still Refused" 就当它们是同族，实测其中一枚带病）。
> **R-137-2（中，AC#2 的开工前提——不先落这条，AC#3 必然做不出来）** —— `assertRefused113` 的调用面只有 **113 五处 ＋ 118 两处**，
> 而 **108 那两枚分母腿的断言是内联的**（`ancestor_separator_108_other_test.go:80-84`、`:138-142`），**一处都不走 helper**。
> ⇒ **AC#2 的范围句改成**：收紧后的判据必须**同时覆盖 helper 与 108 那两枚内联断言**（否则按票面字面只改 helper，AC#3 要求的"11 枚软链形全转红"**必缺 2 枚**）。
> **R-137-5（中，摘掉我票面里一处错引）** —— AC#2 那句"批次 3b 已核过修法是一行"是**错引**：3b 的 `:462` 那句"一行形状"说的是**换根**（＝AC#4 那一形），不是收紧断言。
> ⚠ **要紧的不是账面，是后果**：把"必须一行"当硬约束，最省事的凑法就是**拿被测函数自己算期望值** ⇒ 正踩 AC#2 自己引用的那条红线（`R-119-9`，`119-*.md:73` 终裁方逐字核过成立）。
> ⇒ **删掉"必须一行"这半句约束**，只保留"不许新增依赖／不许放宽成两形都算过／不许拿被测函数算 fixture"这三条。
>
> **AC#4 另有一处已由推断升成读数（终裁方补跑 `tree-mutd-swap`）**：**换根**之后 MUT-D 软链形 **11 枚一枚不落全转红**、普通形逐名差集为空（＝no-op）。
> ⇒ "**换根**"与"**收紧断言**"这两味药**各自都能单独成立、也各自都能单独漏** ⇒ **不许并成一格做掉**（AC#2 与 AC#4 分开交、各自给红名）。
> 代价也量到了：换根之后"未解析那一形"**只剩票 119 那两枚 raw `t.TempDir()` 用例持有**。
>
> **AC#1 翻勾依据**＝本格交付物（"先把成不成立量出来、给逐名表、不是包级 rc"）**已由非实现者独立复现**；
> 结论按事实记为**部分成立**（票面指定那形**不成立**、害在 MUT-D 那形**成立**），**这不是"附条件通过"**——出口只有三种，终裁方原话。
>
> ⚠ **一台仪器异常已登记（影响今晚所有"耗时"类陈述，不影响读数）**：本机墙钟在终裁方两次工具调用之间跳了 **8h32m**
> （三法互证：两枚 commit 戳／台件 mtime／跳变前后各一次 `date -u`）。⇒ **证据与台账里每个时间戳只对它自己那一行成立，
> 任何人不得拿跨跳变的两个时间戳相减当耗时**。我的复核：本机 `date -u` 现为 09-24 00:51 UTC（＝本机 08:51），与终裁开工时刻之差与它量到的同量级 ⇒ 采信为宿主机休眠后回校。

**下游门闸（终裁方独立判，与我一致）**：AC#2 **该起**（先把 R-137-2／R-137-5 落进票面，本块已落）；AC#3 **该起**、形状钉 MUT-D，
另钉三处（对照组改点名／明写禁钉单发 A 与 B／"普通形八数不变"带上参照值 `52/17/13/0 ＋ 17/5/0、rc=1` 且逐名 FAIL 名册与 AB·普通全同）；
AC#4 **该起**且**单独交**；AC#5 维持原判据、等 AC#2 有 `.go` 改动再跑。
