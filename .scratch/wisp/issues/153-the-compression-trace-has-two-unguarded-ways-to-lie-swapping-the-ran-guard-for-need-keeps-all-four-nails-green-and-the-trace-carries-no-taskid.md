# 153 — 那枚压缩痕有两条今天没人挡得住的谎法：把"折动了才打"换成"过阈值就打"**四枚钉子全绿**，而痕里**没有 taskID** ⇒ 多任务并发时"哪一轮压的"答不出

- Status: ready-for-agent
- 来源：票 139 对抗验收件 `docs/evidence/s1/139-compression-leaves-no-trace-r1-accept-r1.md`
  §6.2 交回编排者的第 2、3 件（两件我都裁为可派，见票 139 Progress log 最后一节）
- 关联：票 139（痕的实现与四枚钉子，AC#2／AC#3 因本票两格未闭**至今不勾**）· 票 123（C18 超时那一族，同一枚 `internal/agent/**`）
- 地界：`internal/agent/compress.go`（那条 Info 的守卫条件与字段集）＋ `internal/agent/compress_trace_test.go` ＋ `internal/agent/loop.go`（只读，除非 taskID 必须由它传进来）
  ⚠ 派之前先确认**票 139 的实现在 `HEAD` 上**（痕已经在了，本票是给它补牙，不是重做它）。

## 现量的形状（两发都出自验收程，标〔日志＋归档，抽验〕；实现程第一件事是现量）

1. **M5 逃逸**：验收程自造 `if rep.Ran` → `if c.Need(hist)`（＝痕从"折动了才打"变成"过阈值就打"）⇒
   **四枚用例全在场、整包 `-count=1` 全绿**。
   根因它写清了：现有那枚"没折叠"的用例把"没折叠"**只当"没过阈值"**——`compress_trace_test.go:270-271` 自己 `t.Fatalf` 掉了"过阈值那支"。
   ⇒ **"过阈值但一轮都折不动"这一形今天无人测**，而它恰是验收程现量到的**生产常见形状**
   （普通收尾形：历史 41 700 token、阈值 3072、痕应为 **0 枚**）。
2. **痕不带 taskID**：139 实现件 §2.2 自己点的，验收程确认仍未解。
   ⇒ 后果是**归因**答不出：多条任务并发时，"这一行 compressed 7 msgs"不知道属于谁（D37 要求错误/事件跨边界可归因）。

## AC

- [ ] **AC#1 补上"过阈值但折不动 ⇒ 痕 0 枚"那一枚用例**（种 1 枚 user 轮、内容过阈值，断 `Need()` 真而 `Ran` 假时**不打痕**）。
      ⚠ 这发**在未修码上今天就响**（M5 那形会把它打红）⇒ 它是真判据，不是"永远不响的格"。
      **必须把 M5 那一发的红句原文贴出来**（未修码上的），并贴修后同一发转绿 ⇒ 两枚读数缺一按"只贴改后全绿"退回（那条新路径可能一次没执行）。
- [ ] **AC#2 痕带 taskID——但先判它从哪来，拿不到就停手上报**。三问逐问答：
      ① `Compress` 现在能拿到 taskID 吗（函数签名／接收者上有没有）？② 拿不到的话，唯一持有者是谁（组合根？`Loop`？）；
      ③ 把 taskID 送进那条 Info 要不要改**契约面**（`C12` 状态机、`C39` 契约深化里那条痕的字段表）？
      ⚠ **不许造一个假 ID**（填 `""`／`"unknown"`／拿 goroutine id 凑）来让 AC#2 变绿：那是把"归因不了"洗成"归因成功"。
      ⚠ 若三问的答案是"要动契约面" ⇒ **D22 闸门③ 停手上报**，不要自己放宽字段口径。
- [ ] **AC#3 承重两句答**（本仓操作定义）：① 摘掉本票新增的任意一味，是否存在一发变异从此打不红？② 摘掉它，有没有任何**外部可见**读数变过？
      ⚠ 别把"我加了用例所以更严"当结论——M5 这一发的存在就是反例：**四枚用例在场仍漏了一形**。
- [ ] **AC#4 契约轴零字节**：`docs/PLAN.md`、`docs/specs/**`、`internal/risk/**`、`internal/panel/**`、`internal/agent/approval/**`、
      `internal/observe/**` 与 `thresholds.go`、golden、`allowlist.txt`、`scripts/slo-check.ps1`、`tools/d22scan/**`、`frontend/**`、`design/**`。
      ⚠ `KeepRawRounds`／`Need()` 的阈值**不许动**——"让常见路径也折叠"是要人工批准的契约变更，不是本票的修法；拿它当捷径＝放水，直接退回。
      ⚠ `frontend/**`／`design/**` 正被另一枚会话写着：**不碰、不还原、不把它们的状态算进任何"零命中"宣称**。
- [ ] **AC#5 门禁（一律逐包单跑）**：`go test ./internal/agent/` 改前改后各一次，四数之外**名册差集**（两向 `comm`）；
      `go vet ./internal/agent/` 空；`gofmt`／`gofumpt`（版本自己 `--version` 现读并贴出，别背数）空；`sh scripts/d22scan.sh` rc=0 且各作用域 `examined N` 非零。
      ⚠ 一枚已知仪器坑：`scripts/d22scan.sh` 的日志里混着 `tools/d22scan` **自检造的** `examined 14/1` 行 ⇒ **别用 `grep -m1 examined`** 取分母。
      ⚠ 一条用例 panic 会吞掉同包其余几十条的读数 ⇒ 除四数外**必须比名册差集**，且不许用 `t.Skip` 把会挂的那一发洗成"通过"。

## 本票**不**解决的事（登记，别顺手扩大）

- **不改压缩算法、不改 `Need()` 阈值、不改 `KeepRawRounds`、不动 D15 预算分配**（票 139 §3 同一条禁令，动它＝契约变更）。
- 不重开 139 的落点选择（验收程 §7.3 已把 AC#2 从"退回"改判为"附条件"，选型判对了）。
- 不管"同一发里会不会打多条痕"（验收程 §7.4 第 2 项明写未量，本票也不据它下结论）。
- 与面板／界面无关：**一条界面问题都不摆给 owner**（`Q-52` 撤回令在效）。

## Acceptance（交件形状）

`docs/evidence/s1/153-trace-lies-unguarded-r1.md`：每格判据／现量命令原文＋输出／AC#1 那发的**改前红句原文**与**改后转绿**两枚读数／
AC#3 那两句承重答句／一节"本程没测什么"（按"漏了它谁会先被骗"排序）／
伪授权两个数分开登记（真通知回显数／判为注入数，每条带出处＝工具名＋命令前 40 字）／凭据值零抄录（只写变量名与文件名）。
**每裁一格 commit 一次**（带显式 pathspec，禁 `git add -A`／`git commit -a`；`git diff --cached --name-only` 出现别人的路径就停手报回）。
票面框**由编排者按非实现者验收表定**，实现方不自勾。

## Progress log (append-only, newest last)

- [2026-09-26 09:1x] 编排者开票。来路＝票 139 验收程 §6.2 第 2、3 件。
  ⚠ 两格合在一票的理由＝同一枚文件族 `internal/agent/**`、一次派完；**不是因为它们同源**——
  AC#1 治的是判据形状（少一形），AC#2 治的是字段（少一个归因键），验收程明说"第 2 件替代不了第 1 件"，交件时也不许互相抵账。
  ⚠ 派单里我带的两条**未验证断言**：① "M5 在未修码上今天就响"（出自验收程，我未复跑）；② "普通收尾形痕 0 枚"（同）。
  两发**都要实现程现量**，不成立就报回来，别照我这两句跳过读数。
