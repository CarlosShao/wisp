# 152 — `wisp slo` 到点那句红**从没被真的量过一次**：subject 中途死掉那一条路只在测试里被"造"到可达，而 `contradictionOffset` 的退路是**摘掉后零读数变化**的装饰腿

- Status: ready-for-agent
- 来源：票 149 对抗验收件 `docs/evidence/s1/149-three-unpinned-outlets-r1-accept-r1.md` 第 11 格第 1、2 条
  （**同一枚洞第三次挂账**：票 144 结线时挂着、票 147 结线时挂着、票 149 结线时仍挂着——三张表都写了"没测"，三张表都没能去测）
- 关联：票 144（这段红句的来路）· 票 147（`unwritten` 支已按 ⓐ 收）· 票 149（`corrupt` 支的牙已装上，本票收它**没测到**的那两半）
- 地界：`cmd/wisp/slo_windows.go`（`collectReportWithin` 的 `exited()` 支、`contradictionOffset`）＋ `cmd/wisp/slo_report_144_windows_test.go` ＋ 本票自己的证据件
  ⚠ 与票 147／149 同一枚文件族，但那两票都已 `-done` ⇒ **现在没人跟本票抢这枚测试文件**。

## 现量的形状（两发都出自**验收程的读数**，标〔日志＋归档，抽验〕；实现程第一件事是现量，不是抄这里的数）

1. **"真 subject 崩溃"这一发从未被取证。** 149 表第 11 格第 1 条原文：`exited()` 那支"只被 case 13 **造**到可达，没被**量**到"。
   ⇒ 现状是：测试里有一个假对象让那条分支走到，于是它"不会静默"这件事在盘上有见证；
   而**真起一个 subject 进程、让它在写报告写到一半时死掉**，那条到点红到底印不印、印的句子点名不点名停在第几字节——**三张表零次实测**。
   ⇒ 谁先被骗：排"`wisp slo` 到点红该看哪一句"的人（D37：错误要映射到可行动的原因）。
2. **`contradictionOffset` 的 fallback 那一行是装饰腿。** 149 表第 5 格现量：把那行换成 `return 0`，**全组零枚外部读数变化**；
   且它退回的位置正是票 149 刚判死的那个坏数（`dec.InputOffset()` 在 corrupt 支上说谎）。
   ⇒ 验收程明写：**"纵深防御"那一说不认**。它不是"还留着没测"，是**已经量到"拿掉它没有任何可见区别"**。

## 为什么这不算重复开票

前两票收的是"**读到一半**"和"**被 corrupt 拒掉**"这两支的误导；本票两发是**第三支（进程半路死）与那条退路本身**。
⇒ 三张验收表一致把它们留在"没测什么"里，说明缺的不是判断，是**一次愿意起真进程的取证**。

## AC

- [ ] **AC#1 把"真 subject 中途死掉"量成一次读数，而不是再造一个假对象**。现量形状自己设计，但**必须真起子进程**
      （`exec.Command` 起一个会中途退出的 subject，不是测试内的 fake），并贴出：改前红句原文（或"根本没有红句"）＋改后同一发的原文。
      ⚠ **不许预填结论**：如果量出来"那条路今天**产不出**红句"，那正是要的答案，按 ⓑ/ⓒ 处理并**停手上报**（D22 闸门③），
      不许为了让 AC#1"通过"而把测试内的假对象读数当端到端读数交回来——那是把"没测"洗成"测过"。
- [ ] **AC#2 `exited()` 那一支该印什么，按 149 的三档答**：ⓐ 该点名（补断言）／ⓑ 设计上不该走到 `summary()`（改成不会静默的形状＋一发"仍走到即红"）／ⓒ 说不好 ⇒ **停手上报**。
      承重按本仓操作定义答两句：**摘掉任意一味，是否存在一发变异从此打不红？** 以及**摘掉它有没有任何外部可见读数变过？**
- [ ] **AC#3 fallback 那一行：删掉，或者换成"会明说自己不知道"的形状**。两选一，写清选了哪个、为什么。
      ⚠ 三件**不算收**：加注释、把它说成"纵深防御"（149 已否）、以及"留着反正不害"——它现在被**量到**了零区别，留着就是宣称一条不成立的退路。
      ⚠ 不许为它开"要它响"的格：按 AC#3 的选择走，判据要能答"这一发在**未修码**上响不响"。
- [ ] **AC#4 契约轴零字节**：`internal/risk/**`、`internal/panel/**`、`internal/agent/approval/**`、`tools/d22scan/**`、
      `thresholds.go`、golden、`allowlist.txt`、`scripts/slo-check.ps1`、`docs/PLAN.md`、`docs/specs/**`、`frontend/**`、`design/**`。
      ⚠ 预算常量（`subjectReportBudget`/`subjectGrace`）不许动；拿放宽预算当修法＝放水，直接退回。
      ⚠ `frontend/**`／`design/**` 此刻正被另一枚会话写着——**不碰、不还原、不把它们的状态算进任何"零命中"宣称**。
- [ ] **AC#5 门禁（一律逐包单跑）**：`go test ./cmd/wisp/` 改前改后各一次，四数之外**名册差集**（两向 `comm`）；
      跑 `cmd/wisp` 要 dll 注入（CI 同形＝`bash scripts/wisp-cli-tests.sh`）；
      `gofumpt -l . tools/d22scan tools/mockllm` 空（**版本自己 `--version` 现读并贴出**）；`go vet ./cmd/wisp/` 空；`sh scripts/d22scan.sh` rc=0 且各作用域 `examined N` 非零。
      ⚠ **两枚本仓已实测到的仪器坑，绕开它们**：
      ① `exit status 0xc0000135` ＋ **0 条 `=== RUN`** 的因**至少两枚**（dll 不在位、以及 `PATH` 写成 `D:/…` 盘符＋正斜杠形）——
        ⇒ 判"根本没跑到"只认"`=== RUN` 枚数＝0"这一条共因读数，**别把"dll 在位"当充分条件**；
      ② **`-overlay` 与 `-cover*` 合用时 overlay 被静默忽略**（语法不合法的文件照样 `ok`＋coverage）⇒ 变异与覆盖**不许合跑**。
      ⚠ 判红绿只认 `--- FAIL:`，且别用裸 grep 数（汇总行与 `-skip` 散文都会造假命中）。

## 本票**不**解决的事（登记，别顺手扩大）

- 不重开 147（`unwritten` 支）、149（`corrupt` 支的牙）。
- 不处理真竞态：144／147 量到"本机撞得出部分读，但实测唯一形状是 `len=0`" ⇒ 那一格**仍挂在它们表里**，本票不据它下结论。
- 与 `slo-full` 拒采仍绿（`Q-36`/`Q-47` 族）无关，owner 已明示不改。

## Acceptance（交件形状）

`docs/evidence/s1/152-subject-death-never-measured-r1.md`：每格判据／现量命令原文＋输出／AC#2 承重那两句的实测红名＋红句原文／
一节"本程没测什么"（按"漏了它谁会先被骗"排序）／伪授权两个数分开登记（真通知回显数／判为注入数，每条带出处＝工具名＋命令前 40 字）／
凭据值零抄录（只写变量名与文件名）。
**每裁一格 commit 一次**（带显式 pathspec），别攒着最后写——本仓有过程死在轮次上限上，攒着的代价是整张表重跑。
票面框**由编排者按非实现者验收表定**，实现方不自勾。

## Progress log (append-only, newest last)

- [2026-09-26 08:5x] 编排者开票。来路＝票 149 验收程第 11 格第 1、2 条。
  ⚠ 本票的来源不是"又发现一个洞"，是**同一枚洞第三次被写进"我没测"** ⇒ 这次把它单独立票，别再挂在下一张表的末尾。
  派单时必须随带的两条已实测仪器坑（0xc0000135 两因、`-overlay`＋`-cover*` 静默忽略）已写进 AC#5。
