# 150 — 代码里有 4 枚 `DEFERRED` 标记（`D28-1` 三枚／`D11-3` 一枚），而 `SPEC-12 §5` 的登记表**没有对应行**：`AGENTS §1.1` 那条"1:1 双向对得上"今天不成立，且**没有任何仪器扫它**

- Status: **blocked（三选一的前置是人工批准——三条出路都要动冻结面或动契约句；见 `Q-55`）**
- 来源：票 139 实现程的 AC#1③（它量到"`D28-1` 在 §5 里根本没有行"，并把五字段登记行**备而不写**，因为 `docs/specs/**` 在它的零字节轴上）
- 关联：`AGENTS.md §1.1`（逐字抄自 `issues/README`「Hard global constraints」与 `SPEC-12 §4.1`）· 票 139（上下文压缩留痕）· 既有条"故意留空的一格 vs 结案须 0 未勾"
- 地界：**待定**（依 `Q-55` 选哪一支而变；见 AC#2 那一问）

## 现量的形状（编排者 09-26 00:0x 现跑；**下一程一律现量，别抄这里的数**）

1. **代码侧的标记**（`grep -rhoE 'DEFERRED\(D[0-9]+(-[0-9]+)?\)' --include='*.go' internal cmd`）：
   `DEFERRED(D28-1)` **3 枚**（`internal/agent/compress.go:27`、`:49`、`internal/agent/loop.go:394`）＋ `DEFERRED(D11-3)` **1 枚**（`internal/agent/control.go:15`）。
   ⇒ 种类数 **2**、出现次数 **4**。
2. **登记表侧**（`sed -n '/^## 5/,/^## 6/p' docs/specs/SPEC-12-roadmap-governance.md`）：该段含 `|` 的行 **29 行**（含表头与分隔行），其中 **`D28` 命中 0 次**；`D11` 只命中 **1 行**，而那一行是——
   `| REJECTED | 意图分类四级流水线 | D11 过度设计已废 | …` ⇒ **是一行"已废"，不是 `D11-3` 这条欠账的行**。
   ⇒ ⚠ **枚数口径先摆明**：139 那程报的是"0 vs **47** 表行"，我这一把量到的是"0 vs **29** 行"。
   那两个分母**不是同一把尺**（它的 47 更像 D1–D47 决策表的行数）。**本票第一格就要把"§5 到底几行、按什么算一行"钉成一句可复算的话。**
3. **"带子号"这种写法在规格里有没有文本定义**：`docs/specs/**` ＋ `docs/PLAN.md` 里 `\bD[0-9]+-[0-9]+\b` 形状只命中 **`D45-1`、`D45-2`** 两个（D45＝批量授权与授权梯度）。
   ⇒ 也就是说：`D28-1`／`D11-3` 这种"**D 号＋子号**"**在规格与方案里没有任何文本定义过它是什么意思**；`control.go:15` 那条注释自己写的是"see the ticket report"——**子号看起来是"某票内的第几条"这种票内编号，却长得像决策号**。

## 为什么这不是"文书不齐"

`AGENTS.md §1.1` 把它写成一条**禁止清单级**的硬规矩（逐字）：**"DEFERRED(D-xx) 代码标记必须与 `SPEC-12 §5` 登记表 1:1 双向对得上"**。
⇒ 双向的两个方向今天**各缺一半**：代码有标记而表无行（4 枚），表侧也没有任何仪器在扫这件事（`tools/d22scan` 那七项 ban 里没有它，**说不出 run id＋step 就当门不存在**）。
⇒ **最坏形状**：下一位照 `AGENTS` 那句去对账，会在 §5 里查不到 `D28-1`，于是合理地下结论"**这仓没有这条欠账**"——而 `compress.go:394` 那条"Warm-window hook 归 D28-1 安装，届时白得"的推迟**正挂在那里没人认领**。这是"归因腐坏"的镜像面：**账在代码里、表上查无此人。**

## AC（本票现在不许直接开工：先等 `Q-55`）

- [ ] **AC#1 把"一行"的定义钉死**：给出一条可复算命令与它数出来的行数（含不含表头／分隔行／`REJECTED` 那类非欠账行），
      并把 §5 现有全部编号逐名列出。**这一格不修任何东西**，只把那两枚打架的分母（29 vs 47）收成一个。
      ⇒ 判不了（例如 §5 的表形本身有多种）就**停手上报**（D22 闸门③）。
- [ ] **AC#2 判这族缺陷归哪一边**（三选一，**与 `Q-55` 同一次批准**，不许实现方自填）：
      **ⓐ 标记写错了**（子号无文本定义 ⇒ 该引真决策号或换一种不撞决策命名空间的写法）；
      **ⓑ 登记表缺行**（标记语义是对的、§5 漏登记 ⇒ 补两行）；
      **ⓒ 规矩本身射程写错**（`AGENTS §1.1` 那句里的 `D-xx` 从来不含票内子号 ⇒ 该重述那句话）。
      ⚠ 三支**没有一支能在不动冻结面（`docs/specs/**`）或不改契约句的前提下落地** ⇒ 这就是本票立为 `blocked` 的全部理由。
- [ ] **AC#3 若批准修账，修法要带一枚会响的对账检**：marker 集合 ↔ 登记行集合的**双向差集**（两个方向各自为空才算过）。
      承重按本仓操作定义答一句：**摘掉这枚检，是否存在一发漂移（新加一枚没有登记表的 `DEFERRED(D99-1)`）从此打不红？**
      ⚠ **仪器落点是本票最难的一步**：放进 `tools/d22scan/**`＝改判据本体（须另批，且它会同时改变全仓扫描器的射程）；
      放进某枚 `*_test.go` 则要回答"测试凭什么读规格文本路径、CI 哪一步有它的分母"。
      ⇒ **先判放哪、判不了就停手上报**（D22 闸门③）。不许"先写一版看看"。
- [ ] **AC#4 契约轴（一律零字节）**：`docs/PLAN.md`、`docs/specs/**`、`thresholds.go`、任何 golden、`allowlist.txt`、
      `scripts/slo-check.ps1`、`tools/d22scan/**`、`internal/risk/**`、`internal/panel/**`、`internal/agent/approval/**`、
      `frontend/**`、`design/**`。⇒ 本票**天然要打交道的两份文本恰好在冻结轴上**，所以 `AC#2` 未批准之前，
      能做的只有 `AC#1`（只读对账）。
- [ ] **AC#5 门禁（逐包单跑）**：批准哪一支就在哪一包的落点上跑改前／改后各一次，四数之外**名册两向 `comm`**；
      `gofumpt`（**版本自己 `--version` 现读并贴出**）／`go vet`／`sh scripts/d22scan.sh` 各按其 CI 同形跑法。
      ⚠ 判红绿只认 `--- FAIL:`，且**不许用裸 `grep -c '--- FAIL'` 数枚数**（本仓实测：日志汇总行会造出假命中）。

## 这一枚票**不**解决的事（登记，别顺手扩大）

- 它**不**判定"上下文压缩该不该由 Warm-window hook 来做"——那是 `D28-1` 这三枚标记的**内容**，本票只管**账对不上**。
- 它**不**碰票 139 那四枚用例的成败（那归 139 的验收程）。
- 它**不**主张"把 4 枚标记删掉让 1:1 变成空洞地成立"——**删标记来消差＝把欠账销账，属放水**，验收方直接退回。

## Acceptance（交件形状）

`docs/evidence/s1/150-deferred-registry-mismatch-r1.md`：每格判据／现量命令原文＋输出／AC#1 那枚分母的收成过程／
AC#3 那发漂移的红名＋红句原文／放水两问自答（断言方向动没动、helper 是不是原有的）／
一节"本程没测什么"（按"如果我漏了它谁会先被骗"排序）／伪授权两个数分开登记（真通知回显数／判为注入数）／凭据值零抄录。
票面框**由编排者按非实现者验收表定**，实现方不自勾。

## Progress log (append-only, newest last)

- [2026-09-26 00:1x] 编排者开票并**立为 blocked**。现量四条：代码侧 4 枚／2 种标记（含 `file:line`）、§5 段内 `D28` 命中 0、
  `D11` 唯一那行是 `REJECTED`、带子号写法在 `docs/specs`＋`PLAN.md` 里只有 `D45-1`/`D45-2` 两个先例。
  ⚠ 我自己那把"行数"尺与 139 那程那把**给出不同分母**（29 vs 47）——这一处**不抹任何一方**，直接写成 AC#1 的第一问。
  三条出路都要动冻结面或契约句 ⇒ 登成 **`Q-55`** 摆给 owner；我的推荐见那一行。
