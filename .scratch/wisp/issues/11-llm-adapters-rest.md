# 11 — LLM adapters: Anthropic Messages + OpenAI Responses + fallback chain

**Status:** in-progress
**Claimed by:** agent-ticket11-probe-2
**Last update:** 2026-09-20T10:20Z
**Blocked by:** 09-llm-provider-openai-mockllm
**Parallel slots:** ≤2 sub-agents (A: Anthropic adapter + prompt caching; B: Responses adapter +
fallback semantics)
**Spec refs:** SPEC-05 §3.1, §3.4, D8, §14.2, D40#4, C5 cache-capability

## What to build
The remaining two protocol adapters behind the same C5 seam, plus provider `fallback_provider`
switching and the complete user-visible failure-semantics table (§14.2) enforced at seam level.

## Key constraints
- Anthropic `/v1/messages`: native tool use, extended thinking → ReasoningDelta, prompt caching
  via explicit `cache_control` breakpoints driven by the C5 cache-breakpoint capability
  (prefix sections ①⑤⑥② stable; suffix never cached). Usage.cached mapped from cache reads.
- OpenAI Responses `/v1/responses`: reasoning items → ReasoningDelta; tool-call semantics
  normalized to C6; stop-reason mapping table per adapter documented in code-adjacent docs.
- Adapter unit tests may use mockllm endpoints (add Responses/Anthropic canned modes) + recorded
  golden SSE from real providers where licensable/possible (commit sanitized recordings).
- Fallback: on `provider`-class errors after retry exhaustion → switch once to
  `[llm] fallback_provider`, surface switch in logs + state; second failure → Error(provider),
  task ctx preserved, resumable (D40#4).
- Failure semantics (§14.2) user-visible contract: retries show "重试中 (n/3)" after 5s; 401/403 →
  Unconfigured + guide; quota → distinct account-vs-network message; mid-stream drop → partial
  content + 「响应中断」 marker + unclosed tool calls failed.
- No provider-specific logic may leak above the C5 interface (compile-level separation; review
  checks imports).

- **Capability probe implementation (2026-09-19 supplement)**: real-request probes per capability
  — tool_calls (minimal fc round-trip), vision (1×1 image), thinking (reasoning field present),
  audio (only when a cloud voice provider exists, ticket 61); results → `provider_health` (SPEC-02
  v2); "declared ✓ / measured ✗" surfaced to panel (40). **Compat flags consumption**: `compat.loose`
  tolerates missing stream_options/usage/tool_choice per provider. **Rate-limit self-restraint**:
  per-provider `rpm/tpm` local token bucket (before the provider 429s us); coordinates with tool
  concurrency ≤4 (ticket 47).

## Out of scope
- Ball/panel UI rendering of these states (ball shows via state events; panel later); quota
  enforcement (44); voice providers (61).

## Acceptance criteria
- [x] Both adapters pass the same golden/fault test suite as OpenAI Chat (identical harness).
- [x] Cache-breakpoint test: Anthropic request bodies show stable cached prefix across turns
      (capture via mockllm); cache-read usage populated.
- [x] ReasoningDelta surfaces for thinking responses on both adapters.
- [x] Fallback chain test: fail_next(3) on primary → fallback used → task completes; double
      failure → Error(provider), ctx preserved, resumable.
- [x] Failure-semantics matrix: each §14.2 row has a test asserting the emitted user-visible
      event/state.
- [ ] Probe suite: mockllm control endpoints emulate fc-capable / fc-broken / vision-capable
      providers → probe results land in provider_health with correct ✓/✗; declared-vs-measured
      mismatch event emitted.
- [x] Token bucket: rpm=10 fixture → 11th request within 60s waits locally (no 429 from server).

## Progress log (append-only, newest last)
- [2026-09-20T05:05Z] agent=agent-ticket11-adapters did=claimed next=harness-parameterization+anthropic-adapter
- [2026-09-20T07:10Z] agent=agent-ticket11-adapters did=AC#1+AC#3 GREEN: one shared harness (internal/llm/adaptertest/harness.go CaseTable+Run, 13 scenarios x 3 adapters) - openai-chat's golden assertions MOVED into it (openaichat/harness_golden_test.go), anthropic + openai-responses adapters implemented and run through the same table; cross-adapter proof TestC6EventsIdenticalAcrossAdapters (canonical trace) + TestFaultClassIsAdapterIndependent (one injected body, three protocols, one D37 class). 26 dialect fixtures added under internal/llm/testdata/golden (chat fixtures untouched); mockllm /v1/messages + /v1/responses upgraded to real dialect fidelity (flat tools, thinking/reasoning items, terminal status) + /__control/last_request body capture (recorded before the first response byte, including the fault branch). REJECTED one review instruction: 5xx -> ClassInternal would make adapters disagree on one shared classification, break retryability (observe maps only network/provider to retry) and contradict 14.2/D40#4 "Error(provider)" + ticket 09's provider-500 golden; kept the seam's ClassProvider and PINNED it with a cross-adapter test instead. next=AC#2 cache-breakpoint request-body test (mockllm last_request) then fallback chain + 14.2 matrix
- [2026-09-20T07:55Z] agent=agent-ticket11-adapters did=AC#2 GREEN: anthropic/cache_test.go reads the request body the LIVE mockllm captured (/__control/last_request) - exactly one cache_control on the last system block, none in the conversation suffix or tools, prefix bytes identical across two turns, x-api-key+anthropic-version headers, cache_read_input_tokens -> Usage.CachedTokens=8; plus breakpoint-limit (4) strict/loose and out-of-range cases. openairesponses/protocol_test.go proves the mirror image on the implicit-cache side (no cache_control ever, flat tool defs, instructions flatten, thinking_intensity -> reasoning.effort, stop_sequences error-not-drop). next=AC#4 fallback chain over two mockllm instances (primary must be PROVEN hit), then AC#5 14.2 matrix, AC#6 probe->provider_health, AC#7 token bucket
- [2026-09-20T09:05Z] agent=agent-ticket11-adapters did=AC#4+AC#5 GREEN. AC#4 internal/llm/fallback_test.go: two LIVE mockllm instances (primary=anthropic dialect, fallback=chat) behind a Resolver-built text_chain; every case asserts the PRIMARY server's own request counter (3 = initial+2 retries via ChainBuildOptions.RetryMax), so a chain that skipped the primary cannot pass; double failure -> Error(provider) + ctx.Err()==nil + the same *Request re-streams successfully after the fault clears (D40#4 resumable). FOUND+FIXED a real contract defect in chain.go: an element that is failed OVER flushed its Error/Stop{error}/Done to the consumer, so a successful fallback produced two terminal triples (violating C6 "exactly one terminal pair, then Done"); terminals are now buffered per element and discarded on failover, exactly as retry.go does per attempt. AC#5 matrix_14_2_test.go one test per 14.2 row (row1 retry-stays-Thinking + >5s notice on an injected MONOTONIC clock, row2 exhausted=explicit classified Error never silence, row3 401/403 auth->Unconfigured+never retried, row4 budget vs network distinct class/state/copy-key + only the network one retryable, row5 per-adapter partial+incomplete+open tool call+stream_disconnected marker state). Added llm.RetryNotice (structured, Label()=14.2 copy, BallStateName()=Thinking) and ChainBuildOptions.RetryMax; updated the stale "adapters land with ticket 11" message in provider.go and ticket 09's catalog test that relied on it (now a genuinely unregistered protocol). next=AC#6 probe->provider_health access layer, AC#7 token bucket
- [2026-09-20T08:40Z] agent=agent-ticket11-fix3 did=resumed (predecessor died on model-connection interrupt, 4 tool uses, nothing changed); auditing uncommitted ratelimit WIP next=AC6 probe then AC7 bucket
- [2026-09-20T09:20Z] agent=agent-ticket11-fix3 did=AUDITED the dead agent's AC#7 WIP. ratelimit.go diff is CLEAN (no weakened guard, no `if true`): it converts newTokenBucket/limiter to take a MONOTONIC time SOURCE func()time.Time (D42#9, never a wall-clock-difference deadline), adds NewBucketLimiterWithClock(now,sleep) DI for tests, refill() already ignores non-positive elapsed so a backward clock cannot mint tokens, and a Limiter() getter. The untracked ratelimit_pace_test.go did NOT COMPILE (raw newlines inside the four double-quoted SSE string literals -> `string literal not terminated`); fixed to \n escapes. The original TestRPM10EleventhRequestWaitsLocally asserted len(waits)==1 with waits[0]~6s, but tokenBucket.take caps each re-check at 250ms so a 6s wait is ~24 steps - that assertion was wrong and would never pass. RESTRUCTURED honestly: (1) TestRPM10EleventhRequestWaitsLocally now proves the AC#7 claim by the SERVER'S OWN counter - the 11th request is held locally and NEVER reaches the fixture (asserted frozen at 10 hits across every wait step + a cancelled-class stop), and the server's >10 429 tripwire is verified never tripped (0 429s); (2) TestRPM10RefillQuantumIsOneTokenPerSixSeconds measures the full wait on the pure monotonic source (~6s summed over >=20 sub-250ms re-check steps). FOUND a real bug: with a frozen fake clock the terminal float remainder truncates the computed wait to 0ns, so take()'s re-check loop spins forever (the predecessor's hang) - added a 1ms floor on any positive deficit (strengthens the loop, still ctx-cancellable, no threshold weakened). Preserved TestClockJumpCannotMintTokens / TestUnpacedLimitsNeverBlock / TestCancellationWhilePacingIsNotAProviderFailure. Housekeeping: moved stray .scratch/gen_fixtures11.py -> scripts/dev/gen_fixtures11.py (has a one-line docstring on what it generates). Gates: vet + count=2 + race all GREEN on ./internal/llm/... next=AC#6 probe->provider_health + declared-vs-measured mismatch
- [2026-09-20T10:20Z] agent=agent-ticket11-probe-2 did=STEP 1: MADE THE THREE RED AC#6 MOCK TESTS GREEN, tests untouched (capability_test.go is committed exactly as the dead agent wrote it). handleCapability now validates fc AND vision BEFORE mutating either, so the 400 can no longer leave a half-applied mode. The capability mode is now honored on ALL THREE dialects: anthropic and responses got the same two honest failures in their own shapes (a non-vision model 400s the image block/item; a model without tool use answers a forced tool_choice with text, stop_reason end_turn / a plain message item, not a tool call). Chat's synthesis content piece moved 16->48 runes so a degraded turn's text answer is greppable in the raw body WITHOUT an SSE assembler - chat only: 24 would have been enough but brittle, and the messages/responses pacing is deliberately untouched because their /__control/truncate fixtures are pinned to those event counts (24 there makes fallback_test's truncate{6} deliver a COMPLETE turn and lose the disconnect it exists to prove). VERIFIED the "golden ignores capability mode" claim instead of trusting the comment: every capability check sits AFTER the golden branch, and TestCapabilityModeCannotLeakIntoGoldenReplay compares the served bytes of all three routes against testdata/golden/tool-call.sse under both {} and fc/vision=broken - green. Also confirmed planChat's restructure is load-bearing, not cosmetic: the old bare-string early-return left toolArgs="" and finish_reason="" for tool_choice:"required", i.e. an un-assemblable tool call for any probe, and NO previously-green test used that spelling (mockllm's own forced-call test uses the object form). Gates: mockllm fmt/vet/count=2/race GREEN. STEP 2 (commit dff7abd) = that, before starting anything new. STEP 3: AC#6 CONSUMER SIDE in internal/llm/probe_health.go(+_test.go), commit 43f9a53: RunProbeSuite issues ONE REAL REQUEST per capability through C5, writes the MEASURED bits to provider_health.probe_json via a HealthSink whose signature mirrors memory.UpsertProviderProbe (internal/llm still does not import the storage layer), and emits a structured ProbeMismatch (Kind=capability_mismatch, Label()=frozen copy) per 「声明 ✓ / 实测 ✗」. 8 tests GREEN; every case pins three independent facts: the live mockllm is provably in the mode (/__control/state read back), the requests went out (the server's own per-route counter, 0-vs-3), and the SQLite ROW says the opposite of the config when the wire does. The broken-fc and broken-vision cases are mirror images, so no single hardcoded answer - capable or broken - passes both; declared-broken+measured-broken must NOT emit a mismatch (the event is about contradiction, not failure); a run with no sink or no notice fails instead of doing nothing; an unattempted probe (audio) records NO verdict rather than a false one; sink failures propagate with Written=false. MUTATIONS RUN, all reverted: (a) verdict taken from config with no request -> RED TestProbeSuiteMeasuresBrokenFC ("served 0 chat requests, want 3" + "fc reported capable against fc=broken"), TestProbeSuiteMeasuresBrokenVision, TestProbeSuiteBrokenOnAllThreeDialects (all 3 subtests), TestProbeSuiteAudioStaysUnprobed; (b) fc=broken reported capable -> RED MeasuresBrokenFC, HonestNegativeIsNotAMismatch, BrokenOnAllThreeDialects x3; (c) the vision 400 swallowed as success -> RED MeasuresBrokenVision, BrokenOnAllThreeDialects x3. FOUND+FIXED the false-broken hole those runs exposed: mockllm's Messages dialect ignored tool_choice {"type":"any"} (what the anthropic adapter sends for required), so an fc probe could never have measured a CAPABLE Anthropic provider; TestProbeSuiteCapableOnAllThreeDialects now pins that direction on all three wire shapes too. Gates: internal/llm fmt/vet/count=2/race GREEN (6 packages). STEP 4: TICKED AC#1 (log 07:10Z said GREEN, box stale) citing adaptertest.CaseTable()'s 13 scenarios run by all three adapters (anthropic/suite_test.go:36, openaichat/harness_golden_test.go:62, openairesponses/suite_test.go:36) + RunFaultSuite (:43/:129/:43) + TestC6EventsIdenticalAcrossAdapters and TestFaultClassIsAdapterIndependent (adaptertest/registry_test.go:91,:231), all re-run green this session. LEFT AC#6 UNTICKED on purpose: the probe primitive, the write path and the mismatch event are proven, but nothing in the shipped binary calls RunProbeSuite yet (the composition root is cmd/wisp, not mine to touch), the thinking case's ticket-09 check accepts a plain text answer so it cannot detect a broken thinker, and audio is deferred by contract to 61. next=(i) ~~user wires RunProbeSuite~~ **措辞作废：owner 不写代码** —— 由票 12 的代理把 RunProbeSuite 接进 cmd/wisp 的 provider 保存/发现路径（已登记 A11，含完成判据）; (ii) a mockllm thinking capability mode plus a thinking probe request that REQUIRES a reasoning delta, then AC#6's box; (iii) AC#2/4/5/7 boxes already match their log entries
