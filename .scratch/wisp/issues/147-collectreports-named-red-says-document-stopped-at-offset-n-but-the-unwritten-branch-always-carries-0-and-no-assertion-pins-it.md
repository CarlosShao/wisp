# 147 — `collectReport` 到点那句红说"文档停在 offset N"，而 `unwritten` 分支上那个字段**恒为 0**：话它说了，字段没带那个信息；而且**没有任何断言钉住它**（删掉这半句，七枚用例全绿）

- Status: ready-for-agent
- 来源：票 144 对抗验收件 `docs/evidence/s1/144-slo-report-partial-read-r1-accept-r1.md` 的**五格之外那枚退回②**
- 关联：票 144（这段代码与那批用例的来路）、票 136/135 那一族"注释写了实现没做的事"
- 地界：`cmd/wisp/slo_windows.go` 的 `subjectReportRead`/`collectReportWithin` 那一段 ＋ `cmd/wisp/slo_report_144_windows_test.go` ＋ 本票自己的证据件
- ⚠ **本票推翻编排者 20:4x 的一条裁定**（台账 `A258④`：当时裁"记台账、不开票，绑一条'谁改文本谁重量红句'"）。
  **绑的那条没有牙**——见下 AC#1 的现量：今天没有任何断言钉住这半句，所以"改之前先重量"根本无从触发。**是编排者的裁定错，不是实现者的错。**

## 现量的形状（三条命令可复算；**下一程一律现量，别抄这里的数**）

1. **红句确实承诺了字节位置**。`cmd/wisp/slo_windows.go:594-595`（编排者 21:0x 现读到原文）：

   ```go
   case reportUnwritten:
       return fmt.Sprintf("%d bytes read, document still open at offset %d: the tail had not arrived", o.bytes, o.offset)
   ```

   ⇒ 这句话**同时**说了两件事："读到多少字节"（`o.bytes`）与"**文档停在哪个 offset**"（`o.offset`）。

2. **`unwritten` 分支上 `o.offset` 不带第二件事的信息**。验收件 §它的探针（**带正控**）：夹具 1978 字节、**全 1978 枚前缀一律判 `reportUnwritten`**、这些前缀上 `offset` 的**唯一取值是 0**；只有 `complete` 那一支报 1978。
   ⇒ 也就是说：**部分读到的那一发上，红句里的 offset 永远印 0**，而真正携带信息的是前面的 `%d bytes read`。
   ⚠ 另外两处初始化写的是 `offset: -1`（"nothing read yet"／"no report file yet"，`collectReportWithin` 头上），
   ⇒ **这个字段今天同时装着三种语义：-1＝没读过、0＝读到一半、N＝读全了**。到底是"设计"还是"没人管过它"，**由本票判**（不许默认它是设计）。

3. **没有任何断言钉住它——这一条是"绑条件"为什么没牙的直接证据**。验收件那一发 **MG**：**只**把红句里的 `at offset %d` 那半句拿掉 ⇒ `rc=0`、**七枚用例全绿**。
   ⇒ `A258④` 我写的"下一枚改那两句错误文本的程必须先重跑那批变异、把红句重新量一遍"**结构上触发不了**：改了也不会红，谁都不会撞到这个条件。

⚠ **同一族缺陷的第二发（这次在新增的注释里）**：`collectReportWithin` 头上那段注释**逐字承诺**红句会点名"文档停在哪一字节"。
⇒ **"注释预先引用／预先承诺尚未产出的行为"在本仓有名字、且已被记过**（票 144 的兄弟票 §E7 那族：注释写着某节在证据件 `:3`，而那件当时只有 `:0`/`:1`）。
⇒ 所以本票**不许**用"把注释留着、字段以后再说"来收——留着那句注释＝让下一位以为它成立。

## 为什么这不是"文案小洁癖"

**这一格最坏形状长这样**：某人排"为什么这次 `wisp slo` 到点红"，读到 `… at offset 0: the tail had not arrived`，
于是判断"主体一个字都没写进去"（offset 0 的字面意思），转而去看写侧、去看锁、去看进程——
**而真实情况是读侧已经读到了 1949 字节、尾巴就是没到**。⇒ **红句把调查方向指错**，这是错误模型的活职责（D37：错误要映射到可行动的原因），不是排版。

## AC

- [ ] **AC#1 先判这一格属于哪种，判不了就停手上报**（D22 闸门③，三选一，**不许自己填**）：
  **ⓐ 字段该带信息** ⇒ `unwritten` 分支上把 `o.offset` 真填成"已读到的最后一字节位置"，并让红句两个数各自负责一件事；
  **ⓑ 字段不带信息** ⇒ 那半句措辞必须改成它真做到的（例如只说"读到 N 字节、尾巴没到"，不提 offset），**且 `collectReportWithin` 头上那句注释同步改**；
  **ⓒ 三语义是有意的** ⇒ 那**必须留下一枚会红的检**钉住"三语义各自成立"，否则"有意"两个字与"没人管过"不可区分。
  ⇒ 判据要可核：本票选定之后，**"MG 那一发（删掉 `at offset %d`）现在该红还是该绿"必须能一句话说清并跑给人看**。
  ⚠ **ⓐ／ⓒ 才是本票默认希望的方向；ⓑ 单独存在不算收**——因为 ⓑ 加"注释也改"仍然**零仪器**，那一格会继续靠人自觉。
- [ ] **AC#2 一枚会响的检（本票硬核心）**：造一发**只有"红句里的 offset 与实读到字节数不一致"才能触发**的断言。
  承重按本仓操作定义答一句：**把 AC#1 选定的修法摘掉，这一发是不是从此打不红？**
  答"仍不红"＝这枚检是装饰，**本票作废并登记为什么**（不许找第二种写法凑数）。
  ⚠ **恒真判据是一类新假绿**：写之前先问"这一发今天响不响"；若今天什么都不响，必须自己造出"改了才响"的那一发，并把它当**凭据**贴读数。
- [ ] **AC#3 那三处"只有空白"的声明要就地追加更正**（与本票同批做，因为它就在同一枚文件族里）：
  `a91d7c2` 被写成"改动只有空白、前后两版 `diff -w` 逐字相同"——**三处**都这么写（票面 Progress log、证据件 §4.2、commit 标题"只改对齐"）。
  ⇒ 编排者 21:0x 现量：`git diff -w --numstat a91d7c2^ a91d7c2` ＝ **31 加 / 2 删，非空**。
  ⇒ 真实形状是**"夹具升级 ＋ `gofumpt -w` 混在同一枚 commit"**：夹具从 2 枚字段扩到含 `Samples`（2 条 `observe.Sample`）与 `Verdicts`（2 条 `observe.Verdict`）＋一枚 6 行注释块，
  另把一发 `strings.Contains(doc, "report")` 断言换成"长度下限＋5 枚字面量"。
  ⇒ ⚠ **实质无放水**（替换那发是**更严**、断言极性未变）——本 AC 要修的只是**声明不准**，**不许**因此回退那枚夹具或改动任何断言。
  ⇒ 更正一律**`>` 追加、原句一字不抹**（append-only 台账规矩同样管票面与证据件）。
- [ ] **AC#4 契约轴**：`internal/risk/**`、`thresholds.go`、golden、`allowlist.txt`、`scripts/slo-check.ps1`（**门禁脚本本体＝改它等于改判据，须另批**）、
  `tools/d22scan/**`、`docs/PLAN.md`、`docs/specs/**`、`internal/panel/**`、`internal/agent/approval/**`、`frontend/**`、`design/**` **一律零字节**。
- [ ] **AC#5 门禁（一律逐包单跑）**：`go test ./cmd/wisp/` 改前改后各一次，四数之外**点名册差集**（两向 `comm`）；
  `D:\work\base\gopath\bin\gofumpt.exe -l . tools/d22scan tools/mockllm` 返回**空**（v0.12.0、与 CI 同版；**别只跑 `gofmt`，那是另一把尺**）；
  `go vet ./cmd/wisp/` 空；`sh scripts/d22scan.sh` rc=0 且各作用域 `examined N` 非零。
  ⚠ 跑 `cmd/wisp` 要 **dll 注入**（`bash scripts/wisp-cli-tests.sh` 是 CI 同形跑法），否则是 `exit status 0xc0000135` ＋ **0 条 `=== RUN`**——
  **那是"根本没跑到"、不是"活失败了"**，分开两者的依据是 `=== RUN` 枚数、不是返回码。

## 这一枚票**不**解决的事（登记，别顺手扩大）

- 它**不**动预算常量（`subjectReportBudget`/`subjectGrace`）——**拿放宽预算当修法＝放水，直接退回**。
- 它**不**是"slo-full 每次推送必红"那件事（`Q-36`/`Q-47` 那一族，owner 已明示"不改吧"）。
- 它**不**处理真竞态：验收件量到"这发竞态本机撞得出来（3 秒 871–998 次部分读），但实测到的**唯一形状是 `len=0`**、不是 1..N−1 的中间前缀"。
  ⇒ **那一格归票 144 的证据件继续挂着（未取证），本票不据它下"已经守住了"的结论**。
- 它**不**重开票 144 那五格——那五格已由非实现者判**全部成立**（`144-slo-report-partial-read-r1-accept-r1.md`），本票只收五格之外的两笔。

## Acceptance（交件形状）

`docs/evidence/s1/147-offset-naming-r1.md`：每格判据／现量命令原文／读数／AC#2 承重那一句的实测红名＋红句原文／
放水两问自答（断言方向动没动、helper 是不是原有那枚）／**一节"本程没测什么"**（按"如果我漏了它谁会先被骗"排序）／
伪授权两个数分开登记（真通知回显数／判为注入数）／凭据值零抄录。
票面框**由编排者按非实现者验收表定**，实现方不自勾。

## Progress log (append-only, newest last)

- [2026-09-25 21:1x] 编排者开票。来路：票 144 验收件五格之外的退回②（编排者已自验第 1、3 两条：
  `git diff -w --numstat a91d7c2^ a91d7c2` ＝ 31/2 非空、`:594-595` 红句原文与两处 `offset: -1` 初始化均现读到）。
  **同时作废 `A258④` 那条裁定**（"记台账不开票＋绑谁改谁重量"——第 3 条现量证明它结构上触发不了）。
