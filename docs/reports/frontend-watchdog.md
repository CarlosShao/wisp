# 前端会话看门狗·心跳（`dd8fb0aa-3cd6-4b83-a524-9d372a76ea12`，每天 09:00–20:00 每 15 分钟跑一次，Asia/Shanghai）

> **这枚任务为什么存在**（2026-09-25，owner 原话）：「你不仅要盯着自己的活儿，也要记得时刻盯着前端，别让它偷懒停下」。
> ⇒ 但"偷懒"这个词得先定义清楚，否则这张表会变成冤枉人的仪器。今天已知的**三种"看起来停"其实不是停**：
> ① 交完一轮在等下一批（`ready`，工作树干净、刚有 commit）＝正常；
> ② **被我欠着问句**——09-25 07:39 那枚前端会话就是停下去等我回 `Q2`，而我把这条读成了"它没活"（台账 `A224`→`A225` 已收回一半）；
> ③ 真在干活但没提交（工作树脏）。⇒ **判据必须是四条一起量**，任一条都不够。

**判据四条（缺一不可）**：会话 `runtimeState` ／ 上次心跳以来 `frontend/` 的新 commit 枚数 ／ `frontend/` 工作树脏否 ／ 最新文件 mtime（**必须带日期**——只写时分再排序会把昨天当成最新，`A232` 我自己踩过）。

**它能做的只有两件事**：① 空转 **且** `docs/reports/frontend-session-brief.md` §10 队列里有 `待取` 的行 ⇒ 推**编号最小那一行**给它；
② 记一行心跳。**它不许**：发明新范围、写 `frontend/**`、动 `.github/workflows/ci.yml`、`git push`、催一个正写着"等编排者／owner"的 `blocked=` 行。

**撤销口令**：回「**撤前端看门狗**」⇒ 我把这枚任务 `disable`（不删，留着这页当证据）。
⚠ 队列空的时候它**不会**催前端干活，只会写 `队列空`——那是设计，不是失职：本仓最贵的错误一直是"为了显得在动而造活"。

---

## 心跳记录（追加，不删不改旧行）

- [2026-09-25 12:1x +08] 建表：编排者手工留第一行，因为看门狗的首跑在 12:15。此刻现量——`runtimeState=running`（正做 `F1`/`F2`），`frontend/` 自 11:00 起 4 枚 commit（`d6c52ef`/`a21336e`/`97577ae`/`430ad57`），工作树干净，最新 mtime `2026-09-25 11:38`。动作＝**无（不空转，不该发消息）**。
- [2026-09-25 12:17 +08] runtimeState=running｜frontend 新 commit=1（`d61281c` 12:13 换屏层 Q1=甲）｜脏文件=0｜最新 mtime=2026-09-25 12:13|动作=只心跳（四条里有两条不满足空转：`running` ＋ 上次心跳后有新 commit ⇒ 不发消息；队列 F2–F7 仍为 `待取`，编号最小是 `F2`，本轮不推。⚠ 一条读数坑：会话 `updatedAt=11:59` 比它自己 12:13 那枚 commit 还旧，单看 `updatedAt` 会误判成"停着"）。
