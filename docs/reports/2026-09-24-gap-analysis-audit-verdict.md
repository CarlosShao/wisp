# 缺口文档审核裁决 — `2026-09-23-gap-analysis-vs-oss-harnesses.md` 逐条核真伪

> 作者：编排会话（owner 于 2026-09-23 要求「派人调研开源 harness，找出我们没想到的地方」，
> 2026-09-24 要求「审核一下该文档，看是否有不实之处」）。
> 取证方：6 枚只读代理并发（`核 GAP-01..04 / 05..09 / 10..14 / 15..19 / 20..23 / 24..27＋文档自身章节`），
> 全部**只读**、全部取 `git show HEAD:`，脏工作树未采信。
> 性质：**这是审核裁决，不是判据本身，也不是开票授权。**
> 与 `docs/PLAN.md`（D1–D47）或 `docs/specs/SPEC-*.md` 冲突时以冻结面为准；
> 开票动作按 SPEC-12 §4.1 走人工批准，**本轮一枚票都没有开**。

---

## 0. 一句话总判

**有不实，但不实不在同一层。** 六路复算下来：

- **逐字引用这一层几乎全对** —— 一路代理复算的 20 余枚行号与引文（`PLAN:136/182/306/3202`、
  `SPEC-01 §5.1`、`SPEC-12 §4.1/§4.2/§5:62·77·82·87`、`SPEC-07:51·52·62·63·67`、
  `SPEC-08 §2.1/§3#1/§8`、`SPEC-09 §4#1`、`SPEC-06 §6`、`pending:50/482`）**零枚指偏**；
  计数类 3/3 扣除时移后全部吻合。**这不是一份编出来的文档。**
- **不实在另外三处**：① **数字复现不出**（3 枚）；② **"别人有、我们没有"其实我们有**（4 条，其中 1 条整条不成立）；
  ③ **"可直接开票"这个档位标错了方向**（自称 12 条直接可开，逐核后**至少 8 条一核就撞冻结面**）。
- **最值钱的一条发现不在文档里**：文档 §5 那张「冻结面碰撞表」**漏掉了 D32 资源预算这一整类**，
  而它自己列出的缺口里有 **5 条**（GAP-02/13/25/27/28）会直接撞上
  `Sleeping` 空闲 CPU ≤ 0.5%／RSS ≤ 25 MB／零网络长连接／零周期性磁盘写入（`PLAN:527`）／`Sleeping` 零定时器。
  **表里 0 条命中。⇒ 这张碰撞表不能当 owner 的一次性决策清单用。**

⇒ **结论：可用作「补票的输入」；「冻结面判定」栏与 §6（模板摘要）一律作废重写；引用可按 HEAD 直接复用。**

---

## 1. 取证口径

| 项 | 值 |
|---|---|
| 被审文档 | `docs/reports/2026-09-23-gap-analysis-vs-oss-harnesses.md`，716 行，**28 枚 `GAP-NN`**（01–28 连续无缺号，本轮现量） |
| 文档出处 | owner 于对话中确认＝他让 dsh 做的调研 ⇒ 隔离已解除，但**仍按「输入、不是判据」用**（授权来自对话，不来自文档抬头） |
| 代码事实基准 | `git show HEAD:`（各代理自量锚点，其中 `7d73b5f` 为本轮编排会话锚点） |
| 对标产品事实 | DSH 侧本机可核（`@deepseek-ai/*` 包名与 README）；**MiniMax / Step Code / Claude Code / Codex 四家外部断言本轮无人能本机取证 ⇒ 保持 UNVERIFIED**，本文所有涉及它们的行都不作为裁决依据 |
| 证据档位 | 〔独立复现〕代理自己在 HEAD 上跑出来 ／〔日志＋归档，抽验〕引用台账但抽验过 ／〔仅自述，不背书〕文档或实现方自己说的、没复算 |

---

## 2. 逐条裁决表（28 枚）

> 「真伪」＝它说的「我们没有」这件事成不成立。
> 「判定」＝文档自己标的**可派发性**对不对（`直接可开` / `需 owner 拍板`）。
> 「处置」＝**我**给的归档动作，不自动执行。

| GAP | 真伪 | 最关键的一处不实／漏列 | 文档判定 | 判定对不对 | 处置 |
|---|---|---|---|---|---|
| 01 谁在说话 | **成立**（全仓 `说话人／声纹／voiceprint` 命中 0） | 「就能把屏幕内容念出来」是**现在时、盘上不成立**：`search.content`/`web.fetch`/`clipboard.read`/`system.get` 均未注册（票 22/23/24 open）；①档「浅色壁纸可辨识度」**早已在册**（H6→票 62 已关闭＋Q-1/Q-15 待 owner），它一个字没提 | 需拍板 | 对（但漏了②档实为改 C19＝契约级） | **拆三档**：①并入票 62/68 追加 AC；②③另开票并挂 §5 |
| 02 做错了只能看着 | **成立**（结论我认） | **四处硬伤**：`236 处命中`复现不出（HEAD 实测小写 175 行／混合大小写 245–251）；`SystemProcessSnapshot` 归错文件（实声明在 `internal/proc/treemetrics_windows.go:210`）；把 A20 认定的**死码** `Aggregate` 当现行 hazard 用（与 GAP-04 自述互相打脸）；漏对表 SPEC-12 §5 RESERVED「guardian 双轴评分」——其「当前残缺表现」栏原文正是本条事故形状 | **直接可开** | **错（最该改的一条）**：要动 SPEC-07 §3 D34 表＋SPEC-02 §3＋重读 SPEC-06 §2 L1「非可撤销」措辞 ⇒ 三处都要人工批准 | **开新票，但改标「需 owner 拍板」**；票面须显式声明与 D4 的区分、并如实列退不了的东西 |
| 03 它不会问你 | **成立**（D34 表 33 行工具、20 态，逐字对） | **框定过宽**：漏 D47 Path C（`PLAN:3248-3250` 麦克风常开、realtime 大脑零工具权限**可出声来回对话**）⇒ 真缺口只限 Path T；且 §4 标「直接可开」、§5 却写「owner 批」，**自相矛盾** | 直接可开 | **错**：它自己的论证＝D34＋D43 双契约变更 | **开新票＋改标需拍板＋范围收窄到 Path T** |
| 04 动手前给方案 | **成立** | 「D11 废的是每轮多一次往返的分类器」只核一半：D11 论据①（`PLAN:278-279`）反对的正是「干活前凭空插入一次串行往返」，计划模式同形；漏提 L1 已有 TTS 播报「要做 X」（PLAN:128）这一「事前告知」前身；漏 D32 延迟预算 | 需拍板 | 对 | ①档**并入** S3 的 D45-1 修复票（已被 R14 裁归 S3）；独立 plan 态**缓**，触发＝自用期真出现确认疲劳 |
| 05 出网默认关 | 真但**前提未建**（生产工具只有 `fs.*` 5 枚，`unwired.go:63` 明写无 shell.exec） | 引用全对，但**重**：票 100 **AC#0 触发门逐字就是这一事件**（R21②）；Q-30 已于 09-23 被 owner「都按推荐」批＝不排但保留门 | 直接可开 | **错**：①撞 R21/Q-30；②写进 SPEC-07/插件卡片＝碰冻结契约 | **不独立开票**：②并入票 50/52 的 AC；①标注「已在票 100 门后」 |
| 06 记忆上云前要过滤 | **半真**（`UpsertProfile` 生产零调用者、`memory.save` 工具不存在 ⇒ 今天无数据可漏；但票 29 确无敏感过滤＝设计缺口真） | 「A 档防的是读」不实（契约写「读与写都禁」）；漏了 SPEC-12 §5 DEFERRED「剪贴板历史」前置**敏感识别器**（`PLAN:1525`）同族、且该组件无票 | 直接可开 | **错**：扩 C25 撞 `internal/risk/**`；可用面只有已导出的 `Provenance.CheckText`，而 `CloseScope` 会丢标记 ⇒ 跨会话拿不到旧标记 | **并入票 29**；票面须先答「scope 已关之后标记还在不在」 |
| 07 授权天天要重签 | **真**（引用反而最准：SPEC-12 §5 原句、`PLAN §7:1533`、票 90 验收结论逐条对） | **动机不实**：授权目录＝`[fs] allowed_dirs`（`schema.go:472`→`run.go:305`）**跨重启持久**；每天过期的只有 D45-2 会话档；`ask_every_step` 的 L1 是 2–3 秒自动放行窗口不是重签 | 需拍板 | **对（唯一一条判对的）** | **缓**；票面第一句要先消解「每天重新授权」这个伪前提 |
| 08 可写区里的保护区 | **真**（`blacklist.go:214-232` 9 条 ClassA 逐条数过＝属实；`.git` 只精确匹配 `config`） | **「最坏后果」写错**：覆盖 `.git/index` 命中 **R8→L2**（不可判也置位），R8 在 `mode.go:213` **三档皆不可静音**，SPEC-06 §2 还强制 temp＋原子 rename ⇒ 既非「静默」也非「不被拦」 | 直接可开 | **错**：SPEC-06 头部「黑名单为契约级，不得自行增删」 | **降级需拍板**，或把范围缩到不改 A 档的**工具层守卫** |
| 09 任务级取消没人管 | **真，且是 28 条里最干净的一条** | 几乎零不实（`control_test.go:76-101` 差 2 行；JobScope 全进程一枚、无 `TerminateJobObject` 已复算） | 直接可开 | **半错**：改 `cancelled` 文案＝动 D37 17 类表（契约级） | **可马上开薄票**，但**必须拆掉文案那半**（那半单列为契约变更上报） |
| 10 打字上屏前看不到 | **半真** | 「注入即生效、没有预览环节」**不实**——`input.type` 首次 L2 卡按 §5.4 显示**完整参数**，会话授权后 L1 阻止窗口显示参数摘要且可一键否决，只是被截断；**漏撞**：SPEC-00 §4／SPEC-10 §7.3 锚点写死「连续口述 300 字**直接**出现在光标处」 | 直接可开 | **错**：做预览＝改 D44 验收锚点 ⇒ 实为需拍板 | **缓**；先请 owner 裁场景③那条锚点，落点挂票 37 |
| 11 ASR 热词表 | **真**（全仓只有 KWS 有 `--keywords-file`，ASR 行无词表） | 「这份数据读了就丢」夸大——`system.get` 属 S3，**今天根本没读** | 直接可开 | **错**：票面自带「taint 源做偏置算不算外泄需明确」＝C25/§14.4 未决，**捆了个决定还写「直接可开」** | **并入票 15**（ASR＋CER harness）；taint 那半单列问 owner |
| 12 结果可以念出来 | **真，且零不实** | 无。`PLAN.md:3202`「✅ 已定案」逐字命中；SPEC-05 §7 四档表命中 | 需拍板 | **对，而且把「默认分流不变 vs 新增显式路径」拆开＝符合本仓教训** | **开新票**（S4，紧邻票 30）；只等你在 §5 那一行点头 |
| 13 全屏时自动收起来 | **半真** | 三项提案里**两项已存在**：「手动静默」已有（D43 #8/#10 `Muted`、SPEC-04 §3、`[audio] mic_muted_default`、`audio/gate.go:55`）；「麦克风被占用没人管」不实（`audio/device.go:77 hrAUDCLNTDeviceInUse`＋`inUseGuidance`，D42#12）；`hide_on_fullscreen` 无生产读取者**已被票 83 登记并路由到票 62/65/68+39** | 直接可开 | 勉强对，但与 D42#12「不得静默失败」相冲，票面未提 | **先删掉两项「已有」再谈**；触发＝票 26/30 落地后 |
| 14 干活时能被打断 | **不成立（本次最大的一处问题）** | 全文未提 **D47（PLAN §16.12，09-19 已裁）**：Path C 全双工 barge-in 为 S4 必做、**干活路径保持半双工是设计不是债务**；SPEC-12 §5 已用五字段登记（残缺表现原文＝「干活路径播报中不能语音打断（设计）」）；SPEC-04 §2/§4 有 Path C 例外。§5 碰撞表只写「D43＋D16」⇒ **会让你无感重开一条已裁的 D47** | 需拍板 | 档给对、**内容重复** | **不开此票。** 真缺口另立：`agent.Loop.Steer`（`loop.go:275`）**生产零调用者**，仅 `control_test.go` 用——与票 101/105/114/132 同族 |
| 15 口头宏／技能 | **真**（SPEC-07 §4 manifest 只有 `tools`；全仓 `技能/skill` 命中 0；入口有、存储与口头触发无） | 「AGENTS.md 6 万+项目」「各家都有」＝**外部断言、我方无法核**；**冲突清单漏两项**：技能文本进提示词撞 SPEC-05 §4.1 分段（①⑤⑥②为缓存前缀、合计 ≤2300 tok／D39）＋C25/D30 注入面 | 需拍板 | 对（只列 D3 偏窄，方向保守） | **缓**：等票 50/52 落地且你先定形态；票面须写死「宏名匹配只走 ASR 文本，不扩 KWS `keywords[]`」（否则顶到 Armed ≤90/110MB、CPU≤2%） |
| 16 长期偏好 | **半真** | ⚠「复用 D20 已有的『记住了＋一键撤销』」**码里没有**（全仓无 undo 实现，那正是票 29 未做的部分）；`ProfileManual`（`internal/memory/models.go:34`）确实存在但仅测试调用 | **直接可开** | **偏乐观**：route(a)「L2 提升为常驻规则」**直接撞 SPEC-05 §6 明文＝契约变更**；只有 route(b)（L1 加 manual 槽）不动文本 | **并入票 29 的 A 槽**，且票面**只准走 route(b)** |
| 17 跨会话接续 | **真**（D34 表无历史重建工具；`internal/session` HEAD 只有 `doc.go`） | §3 对照表写「❌无」**过宽**：SPEC-08 §5.2 已白名单 `history.query`/`transcript.get`、票 36 有「task list(30d)→detail」⇒ **面板侧已有设计，缺的只是 Agent 侧**；漏一条：完整接续受 `[privacy] keep_transcript=false`（SPEC-03 §3 硬编码）限制 | 需拍板 | 对 | **缓**（等你答形态）；批了必须写「引用内容按 taint 处理、不得跨会话携带转写」 |
| 18 上下文占用看不见 | **真，而且比它自己写的更强**：`compress.go` 已实现，成功路径**连日志都没有**（只在失败 `Warn`），`res.Compression` 全仓**零生产读取者** | 「12000 token」是 §4.3 原文，但 `budgets.go:36 refHistoryTokens` 按 `context_window` **等比缩放**（D15①强制）⇒ 实际触发点常低于 12000；**撞 A102 前端冻结**（分解条正解落 `frontend/**`，你已交外部 agent）＋票 35（bridge 方法一个都没实现） | **直接可开** | **不实**：新增 Go→前端事件要改 SPEC-08 §5.2 枚举白名单＝契约变更 | **拆两半**：Go 侧先开（压缩成功记 log／接进诊断面，可钉进票 28 的 `DEFERRED(D28-1)` Warm-window hook）；**面板那半退回，不派** |
| 19 「今天干了什么」 | **真**（语音侧无出口：票 26→15、票 30 全 open） | ⚠**「三张表都有数据」只 2/3 真**：`task_log`←`run.go:551` ✅、`tool_call`←`bridge.go:850` ✅、**`cost_daily` 零生产写入者**（`BumpCostDay` 仅测试调用，写入是票 44／S6）⇒ 数字夸大 | **直接可开** | **不实**：唯一干净实现要**动 SPEC-07 §3 D34 权威表**＝契约变更；另与票 47 语义冲突（它的 `task.list`＝调度器状态，非 30 天历史） | **缓**，等票 44/47/26 落地后按**新工具**走 D22 契约流程 |
| 20 包级自检不变量 | **假（本次判得最重的一条）** | 四件对应物已在账：`internal/models/assembly_reachability_121_test.go`（票 121 AC#3，per-GOOS、失败点名包）、`cmd/wisp/leg_dispatch_gate_133_test.go`（票 133 AC#1/#2 入口可达性）、`leg_sink_gate_131_test.go`（腿清单枚举门）、票 71 族 `tools/d22scan` per-scope `examined`＋「empty scope is fatal」；⚠「d22scan `frontend/` 走 0 文件」当现行＝**已过时**（票 88/96 已 done）；「现有对策只有逐票补（票 71）」漏计 121/127/131/133 | **直接可开** | **错**：通用仪器必落 `tools/d22scan/**` 或 `ci.yml`，而 121 票面逐字把它们列为禁改面；报告末段自己说「与 121 同族可复用」却仍标直接可开 | **不开新票，并入票 133**（清单选型＋三枚清单下限） |
| 21 CI 预算与排程 | **真，零处不实**（`A115②` 最近 100 枚 run success 恰 0、`A115③` 4h17m、42 条 staticcheck、17 枚具名红逐枚在 124 分母＝run `35840958334`，台账 4008–4012 行，全部复现） | 无（它自己也标了重复） | 需拍板（明示不需新票） | 对 | **不是缺口，是排程论据**：它给的顺序 122→124→111 正确；补一句 `test-windows` 另有 111/123/128 一支 |
| 22 端到端回归 | **半真**：字面真，但**被测对象不存在**——`internal/session/` HEAD 只有 `doc.go`（票 28 DEFERRED），`SessionScope` 零实现 | ⚠「接缝表全部是单轮或单组件」**不实**：`internal/agent/testdata/golden/budget-loop.sse` 是 4 段脚本响应／3 轮工具的**整环回放**，`loop_golden_test.go` 的 `ToolResultFeedsNextTurn`/`BudgetExhaustionStuck`/`CancellationEndToEnd` 均多轮 | 直接可开 | **错一半**：新黄金可开票，但进门禁＝动 `ci.yml` 形状；新接缝要改 SPEC-10 §2（契约） | **缓**：触发＝票 28 落地并有 `.go` 改动后并进球 54 |
| 23 更新自检 | **半真**（§7.2 判据确实只有崩溃计数） | ⚠说 doctor 含「minisign 公钥可用」检查——`cmd/wisp/doctor.go:45` **只是 info 打印 placeholder**，且不查工具面；「成本极低」不成立＝**更新机制整体未实现**（HEAD 无 `internal/update`、无 updater 源）；漏了 `scripts/build.ps1:161-163` 已把 doctor 当构建后 smoke | 直接可开 | **错**：扩 §7.2 回滚判据＝改 SPEC-11／D41(b)／N-1 契约，SPEC-12 §4.1 须人工批准 | **并入票 54 那格**；判据变更先上报 owner |
| 24 错误文案人话化 | **真**（SPEC-01 §5.1 确 17 类；SPEC-12 §5:77 三字段逐字） | ⚠**漏引 D6**＝`PLAN.md:182` 同行明文「不做…人话化错误文案体系」 | 与现有票重复（标得准确） | 不建票 ⇒ 成立 | **不开票**；若要提优先级，**必须同批销掉 D6 那半句**（契约变更） |
| 25 周期性提醒 | **真，但是重提已裁定的东西** | ⚠`PLAN.md:1803 / 2028 / 3197` **三处**已明文「周期性调度仍 REJECTED」并留痕；SPEC-12 §5:62「REJECTED＝不得被『修复』」——它引了 §5 却没引这句 | 需拍板 | 合规 | **不成立**（没有新论据，只有「对标有」） |
| 26 首次引导 | **真**（`PLAN.md:182` D6、SPEC-08 §3#1、§2.1 逐字） | 与 R6「先自己用」取向相反（未提） | 需拍板 | 对 | **缓**：触发＝R6 撤销／要给第二个人用时 |
| 27 联网先查缓存 | **真**（`pending:50`＝R4 逐行命中；SPEC-06 §6:85 五层、SPEC-07:51/52） | ⚠SPEC-12 §4.2 把「`web.search` 实现路径」列为已知待定案项，未引 | **直接可开** | **错**：动 SPEC-07 §3＋覆盖 R4 默认语义 | **改判需拍板**，或收窄为「默认不变的可选缓存」 |
| 28 用 UIA 读屏幕 | **真但对象不存在**：HEAD 全仓 `UIAutomation` **命中 0**，票 23 未做 | SPEC-09 §4#1、D42#1、SPEC-07:55/62/63 引用都对；未提「走不走 UIA」本身是票 23 的设计决策 | **直接可开** | **自相矛盾**：标「直接可开」，正文自认「specs 是冻结面要 owner 批」 | **不成立为缺口**；转成票 23 的一条设计约束 |

---

## 3. 不实之处归类（回答你问的那句「是否有不实之处」）

**有，共 13 处，分四类。前三类都可复现，第四类是结构性漏判。**

1. **数字复现不出（3 枚）**：GAP-02 的「`internal/` 下 236 处命中」（实测 175／245–251，两种口径都不是 236）；
   GAP-19 的「三张表都有数据」（`cost_daily` 今天零生产写入者）；GAP-22 的「接缝表全部是单轮或单组件」
   （`budget-loop.sse` 是 4 段 3 轮整环）。
2. **把「已有」写成「没有」（4 条）**：GAP-20 **整条不成立**（121/131/133/71 四件仪器已在账）；
   GAP-13 三项提案里两项已存在（`Muted` 态、设备占用引导）；GAP-10「没有预览环节」（L2 卡显示完整参数）；
   GAP-14 重复 D47 已裁方向。另 GAP-17 把「面板侧已有设计、缺 Agent 侧」写成整格「❌无」。
3. **把「要契约变更」写成「直接可开」（8 条）**：GAP-02／03／05／06／08／11／16／18／19／22／23／27／28 里，
   除 GAP-05/08 之外都是**判据自己论证了要改 D34／D43／C25／SPEC-05 §6／SPEC-08 §5.2／SPEC-10 §2／SPEC-11 §7.2，
   档位却仍写「直接可开」**。⇒ 这一栏是本文档**最不能照抄**的部分：照它开票会有大半被对抗验收按「未经批准改契约」退回。
   另有 2 条（GAP-14/28）正文自认要批却**没进 §5 碰撞表**。
4. **结构性漏判（文档 §5 碰撞表）**：漏 4 类，其中要命的是 **D32 资源预算整类缺席**
   （空闲 CPU ≤ 0.5%／RSS ≤ 25 MB／零网络长连接／零周期性磁盘写入／`Sleeping` 零定时器）
   —— GAP-02（快照占空间）、13（自动收起）、25（周期定时器＝**已被 D12 三度 REJECTED**）、27（本地索引缓存）、28（UIA 常驻读屏）
   **五条全撞，表里 0 条。** 另外还漏了 D22 门禁形状。
   ⚠ 顺带一条与本仓现状有关的：GAP-18 的「面板占用分解条」正解落在 `frontend/**`，
   而前端已于 09-23 由你收走交外部 agent（A102）⇒ **文档没有「这条我们不该自己做」这一栏。**

**还有 2 处属「写作时刻的正常漂移，不算不实」**：§1 的「136 张票（58 done / 78 open）」现值 **137 / 59 / 78**
（票 131 在 09-23 21:33 转 `-done`、票 137 在 21:39 新建，都晚于它的写作时刻）；
GAP-24/25/26 引用的行号全部命中。**根因是它没声明自己的快照时刻与 HEAD sha**——这正是它 §7 自我披露该补而没补的一条。

---

## 4. 处置分档（28 枚全落档，一枚没开）

| 档 | 枚数 | 条目 |
|---|---|---|
| **A 不成立／别开** | 5 | GAP-14（重复 D47）、GAP-20（假，四件已在账）、GAP-21（不是缺口是排程论据）、GAP-25（重提三度留痕的 REJECTED）、GAP-28（对象不存在） |
| **B 并入现有票，不新建** | 8 | GAP-01①→票 62/68；GAP-04①→S3 的 D45-1 修复票；GAP-05→票 100 门后＋票 50/52；GAP-06→票 29；GAP-11→票 15；GAP-16→票 29（只准 route(b)）；GAP-19→票 44/47/26 之后；GAP-20→票 133；GAP-23→票 54 |
| **C 缓＋写死触发条件** | 7 | GAP-07、GAP-10、GAP-13、GAP-15、GAP-17、GAP-22、GAP-26 |
| **D 真缺口，但需你拍板才动** | 6 | **GAP-02**（改判需拍板）、**GAP-03**（收窄 Path T）、**GAP-09**（拆掉文案那半）、**GAP-12**、**GAP-18 Go 侧那半**、GAP-08（或缩到工具层守卫） |
| 附：需先销冻结面半句才能提优先级 | 2 | GAP-24（销 D6 半句）、GAP-27（收窄为「默认不变的可选缓存」） |

⚠ **B 档里 GAP-20/23 我算了两次的说明**：GAP-20 主档是 A（缺口不成立），「并入票 133」是它可取那一小块的落点；
不重复计数。按主档统计：**A 5 / B 8 / C 7 / D 6 / 附 2 = 28**。

**档 D 内部还可再分一层（这是唯一一个我建议「不用等你点头就能动」的形状）：**
- **GAP-09 拆半 + GAP-18 的 Go 侧那半** —— 六路审核一致认定：不撞 D1–D47、不撞 `frontend/**`、不动阈值、不动 golden。
  其中 GAP-09 的审核原话是「**可直入排程**」，GAP-18 的 Go 侧原话是「唯一可原样进排程」。
- 其余四枚（02 / 03 / 12 / 08）都要动 D34 表、D43 表或 SPEC 契约 ⇒ **一枚都不许先动**。

---

## 5. 文档自身章节要修的地方（若要入库）

| 章节 | 状态 | 要做什么 |
|---|---|---|
| §2 一句话结论 | 站得住 | 「一条轴做得细、另一条轴几乎空白」这个判断我认，六路没有推翻它 |
| §3 对照速查表 | 4 格「❌无」需改写 | GAP-17/18/19/13 那几行是「具体交互没做」被写成「能力不存在」；需按本文 §2 表逐格加注 |
| §4 各条「可派发性」栏 | **一律作废** | 以本文 §2 表第 5 列为准 |
| §5 冻结面碰撞表 | 9 条全真、无凑数，但**漏 4 类** | 至少补：D32 资源预算、D22 门禁形状、SPEC-12 §4.2「web.search 实现路径待定案」、A102 前端已交外部 |
| §6 写票模板 | **摘要不全，不可照抄** | 8 条 forbidden patterns／规则 6／规则 7（含「按目录不按票号」）逐字对、模板 6 字段＋5 节与真票吻合。**但**：①出处标错（自称「摘自 README」，`D1–D47/C1–C32` 实为 SPEC-12 §4.1 值，README 真身当时写 `D1–D46/C1–C31`）；②**漏 6 条要命铁律**：裁决表须≠实现者、规则 4「`-done` 改名＝防重领唯一键」、规则 8「临时件只建不删」、commit 显式 pathspec／禁 `add -A`／禁 `--amend·reset·rebase·stash·checkout .`、D22 mode-6 四禁令、阈值/golden 一字不动；③漏抄规则 7 的**「不冻结那半」**（README 明警「别扩大解释」） |
| §7 自我披露 | 属实且难得 | 补 2 条即完整：声明快照时刻与 HEAD sha；声明仓内引用的复核覆盖度（它只对外部调研标了 UNVERIFIED） |
| §8 引用 | 2 处对不上 | `audit-A` 抬头是 **09-19** 不是「09-20」（只有 B 是 09-20），且两者对象是「13 张 done 票」非全仓完成度；injection-timeline 自陈「**五代已定性＋第 6 代候选**」，写「7 代形状演变」偏重（现亦已到第 8 代） |

---

## 6. 本次审核顺带量到的**仓内真腐坏**（不是文档编的，我现量过）

这些跟文档无关，是核文档时撞出来的**我们自己仓里**的洞：

| # | 事实（现量，可重跑） | 在哪 | 冻不冻结 |
|---|---|---|---|
| 1 | SPEC-12 说定案数是 `D1–D47 / C1–C32`，`PLAN.md:3` 自己还写 `D1–D46 共 46 条 + C1–C31 共 31 条` ⇒ 抬头少 1 | `docs/PLAN.md:3` vs `docs/specs/SPEC-12-ai-coding-lifecycle-integration.md:38` | **冻结** ⇒ 需你批准 |
| 2 | SPEC-08 §3 表头写「20 态 **40** 条转移」，实测表体 **42 行**（#1–#42 全不重复，41/42 是 D47 加的）⇒ 表头过期 | `docs/specs/SPEC-08-ui-ball-panel.md:85`，行数在 `7d73b5f` 现量 | **冻结** ⇒ 需你批准 |
| 3 | SPEC-12 §4.2 明文要求「Agent 执行版：**薄** `AGENTS.md`（根：D1–D47 一行摘要＋禁止清单＋未定义即停＋docs 索引）」，**仓根没有这个文件**（`AGENTS.md`/`QODER.md`/`CLAUDE.md` 三枚 `ls` 全部 No such file）⇒ 每一枚新代理都要重新自己发现铁律，本次 6 份审核简报里都有「铁律我是从 HANDOVER/A34 拼出来的」这类话 | 仓根 | 新建 ⇒ 需你批准（SPEC-12 已授权其形状，未授权 agent 自造内容） |
| 4 | `.scratch/wisp/issues/README.md` 规则 1/2 原文写「commit + push (both remotes)」，与全仓实际纪律（子代理只 commit、推送由编排者核后串行做两远程）**互相打反**；且全仓**没有单一权威文本**写这条 | README:19 | **本轮我已改掉**（含 `D1–D47/C1–C32` 那行同步对齐 SPEC-12） |

⇒ #1/#2/#4 属「引用会腐坏」这一族的老账；#4 已闭，#1/#2/#3 在你手里。

---

## 7. 需要你的（4 条，都给了推荐；不答的代价写在后面）

**没有一条我已经动手，包括第 5 条那两枚「不用批准」的也没开——因为开票本身就是动作。**

| # | 事项 | 选项 | **推荐** | 人话后果 | 不答的代价 |
|---|---|---|---|---|---|
| Q-39 | 这份缺口文档要不要**入仓** | 入（顶部加「§4 可派发性栏与 §6 摘要作废，以本裁决为准」一行）／继续留在工作树不入 | **入，并加那行作废声明** | 入仓后任何代理都能引用它补票，代价是必须同时引用裁决表否则会被那 8 个错档位带跑 | 不入 ⇒ 它只在我这台机器上，下次会话看不见，等于这次调研白做 |
| Q-40 | `PLAN.md:3` 的 `D46/C31` 与 SPEC-12 的 `D47/C32` 哪个作准 | 改 PLAN.md:3 为 47/32 ／ 不改 | **改 PLAN.md:3**（PLAN.md 是冻结文件，所以要你点头） | 改后代理读抬头就是对的；不改则每枚新代理都要自己去 SPEC-12 核对一次 | 不改 ⇒ 一直有代理引错数字，且对抗验收会按「引用腐坏」反复退回 |
| Q-41 | `SPEC-08:85` 表头「40 条转移」实为 42 行 | 只改那一行数字为 42 ／ 登记但不动 | **登记为 SPEC-08 的一处纯计数更正，不动任何转移语义**（SPEC 是冻结面，改动范围我会限定到只有那一行） | 只改一个数，行为一字不变；这条表本体「未列出即非法」是判据，必须精确 | 不改 ⇒ 谁按 40 去数都会以为自己数错，或漏看 #41/#42 两行 |
| Q-42 | SPEC-12 §4.2 要的**薄 `AGENTS.md`** 到底建不建 | 建（严格按 §4.2 那六个字段，内容全部从现有权威文本抄、不新造规矩）／改 SPEC-12 取消这一条 | **建** | 以后每枚代理一进来就看到铁律（含 pathspec／禁 add -A／裁决方≠实现方／临时件只建不删），本次 6 份简报里那种「我自己拼出来的」就不会再出现 | 不建 ⇒ 铁律继续散在 README＋HANDOVER＋A34 三处且互相打反，新代理靠运气发现 |
| Q-43 | 档 D 里那两枚「不撞冻结面」的（**GAP-09 拆半**、**GAP-18 Go 侧那半**）要不要我现在开票并排进队列 | 都开 ／ 只开 GAP-09 ／ 都别开 | **都开**，排在今日队列之后（现在前面还有 137 AC#3/#4、135 原六格、136 AC#10/#11、#40 全树终判据复算） | GAP-09＝你按停止之后，那一轮任务自己起的子进程今天没人负责关掉；GAP-18＝上下文被压缩掉多少，今天你**完全看不到**（连日志都没有）。两条都不改任何已定行为 | 不开 ⇒ 这两条继续只是「文档里的一句话」，而它俩恰好是本次 28 条里唯一不需要任何人批准就能做的 |

一句话答法：**「都按推荐」**＝Q-39 入仓＋Q-40 改 PLAN:3＋Q-41 登记纯计数更正＋Q-42 建薄 AGENTS.md＋Q-43 两枚票都开。

---

## 8. 本轮登记（不作依据）

- 六路审核**均未遇到**指向改判据／回滚／放宽阈值的伪授权文本；出现的 4 处 harness 噪声
  （技能清单 `<system-reminder>`、"MEMORY.md 已被修改"回显）**全部未采信为授权**，只登记。
- 凭据值零抄送；本轮无需脱敏反扫（六路输出我逐枚读过，未出现密钥形状）。
- 外部对标产品（MiniMax / Step Code / Claude Code / Codex / Gemini）的断言本轮**无人能本机取证 ⇒ 全部保持 UNVERIFIED**；
  只有 DSH 侧因本机有 `@deepseek-ai/*` 包与 README 而成〔独立复现〕。
- 编排者自纠 1 处：我在 09-23 对 owner 说过本文档「27 条 GAP」，**现量为 28**（`#### GAP-` 标题 28 枚、
  `GAP-\d{2}` 去重 28 枚，编号 01–28 连续无缺号）。错的是我的读数，不是文档。

---

## 9. 入库后的自纠与执行回执（append-only，§2/§6 原文一字不改）

### 9.1 ⚠ 本文件 §6 表第 3 行的**出处指偏**（我自己写错的，不是审核代理写错的）

§6 表第 3 行我写的是「**`SPEC-12 §4.2` 明文要求**『Agent 执行版：薄 `AGENTS.md`』」。**章节号错了**：

- 真身＝**`SPEC-12 §6`「文档交付物清单」**，逐字原文在 `docs/specs/SPEC-12-roadmap-governance.md:102`
  —— 「Agent 执行版：**薄** `AGENTS.md`（根：D1–D47 一行摘要 + 禁止清单 + 未定义即停 + docs 索引）」。
- `SPEC-12 §4.2` 是**另一件东西**：「未定义即停（D22 闸门③）」那张六条待定案清单
  （`:40-49`）。⇒ 那条引用**指偏到了隔壁章节**，而 §4.2 里确实有「`web.search` 实现路径」，
  所以我这一句**看起来像成立的**——这正是本仓登记过的「编号要查占用／引用会腐坏」那一族的**新变体**：
  **不是引到不存在的章节，是引到存在但不管这件事的章节。**
- ⇒ 定式：**引用某条规矩时，锚点要落到"那句规矩的字面"，不要落到"那个编号"**；
  核销动作＝`grep -n` 那句字面、看它落在哪个 `##` 之下。本次已按此重走。
- ⚠ 同一处**不改原文**（本文件已随 `7a8403f` 推送），更正只写在这里。

### 9.2 owner 批复与执行回执（09-24 10:0x，原话「**都按你的推荐来**」）

| 条 | 我的推荐 | 执行到什么程度 | 怎么回来 |
|---|---|---|---|
| Q-39 文档入仓 | 入＋加作废声明行 | **已入**（顶部叠加一段作废声明，**正文一字未改**） | 「缺口文档撤出仓库」 |
| Q-40 改 `PLAN.md:3` | 改 47/32 | **已改**，`git diff` 逐字节核对＝**只动数字、1 增 1 删** | 「PLAN 抬头改回 46/31」 |
| Q-41 改 `SPEC-08:85` | 登记为纯计数更正 | **已改**（`40 条转移`→`42 条转移`，**转移语义一字未动**）。⚠ 改前多量了一步：去注解后表体**仍是 20 枚裸状态名** ⇒ "20 态"没腐坏，只有条数腐坏，所以那一行**只该改那个数** | 「SPEC-08 表头改回 40」 |
| Q-42 建薄 `AGENTS.md` | 建，内容全抄 | **已建**（157 行；D 表 47 枚由脚本从 `PLAN.md` 标题现抽、**无缺号**，其余四节逐字抄 `SPEC-12 §4.1/§4.2/§4.3/§6` 与 `issues/README`「Hard global constraints」，**没有一条是我新造的规矩**，且抬头写明"与权威文件不一致时以权威文件为准"） | 「删掉 AGENTS.md」 |
| Q-43 开两枚薄票 | 都开 | **已开**＝**票 138**（GAP-09 拆半，文案那半划出去）与**票 139**（GAP-18 Go 侧那半，面板那半明令不做）。⚠ 两枚都排在今日队列**之后**，**未派代理** | 「138 撤」／「139 撤」 |

### 9.3 建票前自己现量、结果**修正了审核代理一句**的地方

审核代理报 GAP-18 时说 `compress.go`「成功路径连日志都没有（**只在失败 Warn**）」。我自己 `git grep` 走一遍：
`internal/agent/compress.go` 全文**零条日志调用**——那条 `Warn` 在**调用方** `internal/agent/loop.go:398`。
⇒ 缺口比审核代理报的**还深一层**（不是"成功没日志、失败有"，而是**那个文件从头到尾不产日志**）。
已按实测写进票 139 §0，并在票面标明这是**对审核代理措辞的修正**。
⚠ 同族旧账：「派单里的否定式前提同样是未验证断言」——这次是**肯定式的**读数同样要自己走一遍。

### 9.4 ⚠ 新增一条待人拍板：**同一个过期计数不止我报的那两处**

`Q-40` 我按 owner 批准的**字面范围**只改了 `PLAN.md:3`。落笔前 `git grep` 现量到**另有 4 处**写着一样的 `D1–D46 / C1–C31`：

| 位置 | 是什么 | 我动没动 |
|---|---|---|
| `README.md:10`（仓根） | docs 索引里对 `PLAN.md` 的一句描述 | **没动**（不在批准范围内） |
| `docs/specs/README.md:3` 与 `:8` | spec 目录首页，其中 `:8` 是**优先级声明**（「PLAN.md 定案内容 D1–D46／C1–C31 —— 最高，spec 不得与之矛盾」） | **没动** |
| `docs/specs/SPEC-00-product-overview.md:3` | 追溯行 | **没动**（冻结切片文件） |
| `docs/evidence/s0/01-adversarial-acceptance.md:122` | 历史裁决表里那句"禁修改 D1–D46/C1–C31" | **永不改** ——那是**当时的读数**，改证据＝改写历史 |

⇒ 已登记为 **`Q-44`**（见台账 Q 表）：前三处要不要一起对齐。**我不擅自扩批准范围**——
这次是"数字笔误"所以看起来无害，但**同一形状下一次会是"我以为他会同意"**。

